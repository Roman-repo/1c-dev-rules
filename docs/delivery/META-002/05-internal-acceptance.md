# Отчёт внутренней приёмки — META-002

**Дата:** 2026-09-05 · **Задача:** вливание metadata-слоя в обёртку ревью (эпик EP-001)

**Самопрогоны разработки (шапка, гейт 4→5):**

- `python3 -m unittest discover tests` — **285 тестов OK** (было 268; +17: 11 слияние/CLI обёртки, 1 битый XML, 5 кэш индекса)
- `python3 scripts/validate_skills.py` — PASS, 0 WARN
- `python3 scripts/check_version_sync.py` — 0.33.0 синхронно (plugin/marketplace/kimi/README, bump minor)
- Детерминированные слои к коду плагина (.py) неприменимы; живой прогон обёртки — критерий 1

## Прогон критериев (матрица 04)

### Критерий 1 — дифф с .bsl И .mdo одним прогоном → один отчёт

- **Unit:** `TestMetadataMerge` (11 тестов) — автозапуск, метка слоя, карточки каталога/локальные поля.
- **Живой прогон** (`toir2`, ветка задачи, `--diff HEAD --src-root Project/Toir/src --slim-config --cache-dir …`): **одна команда → 39 находок (🔴3/🟡13/🟢23)** от трёх слоёв; шапка отчёта: «…файлов разобрано: 2627 + слой 1 (`checkbsl_scan.py`, слито с дедупликацией) + слой метаданных (`metadata_scan.py`, слито)». Metadata-находки: 11 (в т.ч. 🔴 2 × `QueryParamMismatch` — те же истинные, что в прогоне META-001; 🟢 `MetadataSynonym` с карточкой docs.checkbsl.org, `LocalTypeUnknown` подсистем). Отчёт: `toir2/docs/code-review/2026-09-05-meta002-merged-layers/bsl-ls-r1.md` (правило двух отчётов: ревью — в проект системы).

### Критерий 2 — дедуп и подавления действуют на metadata-слой

- Unit: `test_merge_layers_priority_metadata_first` — коллизия трёх слоёв погашена (одна находка, метка metadata); `test_cli_suppress_covers_metadata` — suppress.json глушит `MDOWithoutSynonym`, счётчик «suppressed: 1».

### Критерий 3 — отключение флагом как у слоя 1

- Unit: `test_cli_no_merge_metadata` (счётчик 0/0/0), `test_cli_merge_metadata_ready_json` (`--merge-metadata FILE` — готовый json, находки в отчёте).

### Критерий 4 — счётчик покрытия отражает metadata-ключи

`--coverage-report` (живой вывод):

```
Слой метаданных (metadata_scan.py, каталогные из CHECKS): 16 ключей
Вместе (1 ∪ 2 ∪ metadata): 149/322 = 46,3% [из покрытых: overall 120 / query 11 / metadata 18]
```

Базлайн «до» (счётчик main 2026-09-04): 135/322 = 41,9%, metadata 4/32 → **149/46,3%, 18/32**. Замок `test_stats_shape_and_baseline` (149/46,3/meta_layer 16/секция 18); README+ARCHITECTURE+докстринг синхронизирует `test_readme_and_docstring_in_sync` (в сьюте, зелёный).

### Критерий 5 — [Откл 2а] дифф только с .mdo

- Unit: `test_cli_diff_mdo_only_no_early_clean_exit` (tmp git-репо): rc 1 по 🔴 metadata, BSL LS не запускается (флагтивные --java/--jar не задеты), stderr «BSL LS пропущен». Прежний ранний выход «чисто» устранён.

### Критерий 6 — [Откл 3а] нет Java

- Unit: `test_exit3_message_names_both_layers` — exit 3, сообщение называет `checkbsl_scan.py` И `metadata_scan.py` (+ обновлён `test_exit_3_when_tools_missing`).

### Критерий 7 — кэш индекса

- **Механика (unit, 5 тестов):** hit (сентинел из кэша), инвалидация по mtime (правка дерева → новый ключ, обход заново), битый файл → обход, без `--cache-dir` — файлов нет, запись при первом обходе.
- **Замеры (toir2, дерево 500 МБ / 5257 XML):** standalone metadata-повтор с кэшем **0,57 с** (tree_key 0,26 с + загрузка индекса 0,37 с; было 9,7 с обхода); инкремент metadata-слоя в обёртке **+0,8 с** (2,5 → 3,3 с). Холодный прогон обёртки 89 с (строит оба кэша; java-анализ ~70 с доминирует).
- ⚠️ **Отступление от исходной формулировки «полный повтор ≤2 с»:** цель имела неверный базлайн — тёплый прогон обёртки и БЕЗ metadata-слоя ≈2,5 с (загрузка кэшированного отчёта BSL LS 95,7 МБ ≈1,1 с + импорты; пустых fileinfos только 482/2627 — фильтрация дала бы 6%, не решение). Предмет задачи (инкремент слоя) в цель укладывается; уточнение критерия — история изменений 04, кандидат оптимизации — производный кэш находок по диффу (вне рамок). **Выносится на Ревьюера (05a).**

### Критерий 8 — [Откл 5а] приоритет коллизий

- Unit: metadata → слой 1 → BSL LS (тройная коллизия — выживает metadata; парная слой1/BSL LS — слой 1, семантика `merge_layer1` сохранена).

### Критерий 9 — [Откл 6а] битый XML

- Unit: `test_broken_xml_finds_warn_not_crash` — `LocalBrokenXml` 🟢 в общем отчёте, остальные источники дают 🔴, обёртка не падает.

### Критерий 10 — регрессия

- Сьют 285 OK; `checkbsl_scan.py` **не изменялся** (git diff: нет в списке); standalone-CLI `metadata_scan.py` без `--cache-dir` — прежние 52 теста META-001 зелёные без правок проверок; drift-замки (`local_cards_check`, ALIAS) — в сьюте, зелёные; `validate_skills` 0 WARN. Изменённые файлы — строго список 03 (+манифесты версий и артефакты конвейера задачи).

## НФТ (замеры, 03)

| Требование | Факт | Вывод |
|---|---|---|
| Повторная итерация петли с `--cache-dir` ≤2 с | полный прогон обёртки 3,3 с (без metadata-слоя 2,5 с; инкремент +0,8 с; standalone metadata 0,57 с; до задачи ≈10 с) | НФТ целила не тот слой: доминирует загрузка кэша BSL LS (95,7 МБ) — вне рамок задачи; уточнение критерия 7 в 04, кандидат — производный кэш по диффу |
| Без кэша: +≈9,7 с к прогону обёртки | холодный прогон 89 с (java ≈70 с + обход ≈10 с + парсинг) | в пределах оценки 03 |
| Совместимость: standalone-CLI без `--cache-dir` прежний; json/md обратно совместимы | 52 теста META-001 зелёные; json расширен полями (ls_code-метка), схема не ломалась | ✅ |
| Кэш — не источник истины | unit: битый файл → обход; правка mtime → инвалидация | ✅ |

## Наблюдения живого прогона (кандидаты в плагин, вне рамок задачи)

1. Кэш отчёта BSL LS — 95,7 МБ на toir2 и ≈1,1 с на загрузку: производный кэш «находки по диффу» (ключ: хэш отчёта + ref + файлы) срезал бы тёплый прогон обёртки до долей секунды — кандидат в отдельную задачу оптимизации петли.
2. `NonExistentMethod` (BSL LS, 🔴) на новом коде TASK-002 — ожидаемо для «вслепую» написанных вызовов до импорта EDT; подтверждает ценность петли на живом ревью.
3. Слой 1 в режиме путей теперь фильтрует файловые входы по `.bsl`/`.os` (CHANGELOG «Изменено»): убран ложный `HardcodedGUID` на uuid внутри `.mdo` — попутный фикс в рамках 03 (семантика слоёв).

## Вердикт внутренней приёмки

Критерии 1–10 пройдены (✅×5, ✅с×5 — статические/синтетические полигоны без живой базы 1С). Одно отступление от числовой НФТ (критерий 7 «≤2 с») — с разбором и уточнением формулировки в истории 04, выносится на решение Ревьюера вместе с 05a. Красных строк нет → задача идёт на этап 6 «Код ревью».
