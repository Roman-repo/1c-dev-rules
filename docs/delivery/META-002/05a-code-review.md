# Лист код-ревью — META-002

## Решение

- [x] **Одобрено** — код прошёл ревью, переход на «Внешнюю приёмку» (`1c-external-acceptance`)
- [ ] **Доработать** — замечания в таблице ниже; возврат на «Разработку», правка, повторное ревью (этот же файл)

**Режим ревью:** manual
**Ревьюер:** Roman — решение по PR #91 в сессии ZCode (дифф PR + сводка внутренней приёмки 05)
**Дата:** 2026-09-05

## Замечания

| № | Замечание | Правило/Стандарт | Место | Статус |
|---|---|---|---|---|
| — | Блокирующих замечаний нет. Позиции внимания, принятые Ревьюером с решением «Одобрено» (из 05): (1) отступление от числовой НФТ критерия 7 — «полный тёплый прогон ≤2 с» имел неверный базлайн (обёртка без metadata-слоя ≈2,5 с: кэш отчёта BSL LS 95,7 МБ ≈1,1 с); инкремент задачи +0,8 с, standalone 0,57 с — в цель укладывается; уточнение формулировки — история 04; кандидат оптимизации — производный кэш по диффу (отдельная задача); (2) `main()` обёртки стал ветвистее (`bsl_needed` + три слоя) — прокомментировано, вынос в класс CLI посчитан оверинжинирингом; (3) изменение контракта выхода: несуществующий путь без Java — exit 2 вместо 3 (валидация входов до проверки Java), зафиксировано в CHANGELOG «Изменено»; (4) слой 1 в режиме путей фильтрует файловые входы по `.bsl/.os` — попутный фикс ложного `HardcodedGUID` на uuid в `.mdo` | 03 НФТ / границы | scripts/bsl_ls_analyze.py (main, merge_layers); CHANGELOG «Изменено» | ✅ |

Правила:

- «Доработать» без строки замечания — бессмысленно: исправлять нечего.
  Каждое замечание — про конкретное место кода (файл:строка) и конкретное
  правило (ключ из каталога `references/checkbsl/` или № стандарта — как в
  чек-листе скила).
- Ревью проверяет **качество кода по стандартам**, не требования: спор о
  требованиях — не замечание ревью, а возврат с внешней приёмки по классам
  A/B (`1c-delivery-gate` §3).
- Замечания по правилам с важностью 🔴 (чек-лист, каталог checkbsl) —
  блокирующие: «Одобрено» при открытых 🔴-замечаниях не ставится.
- После правки Ревьюер повторно принимает решение этим же файлом: статусы
  замечаний → ✅, решение обновляется. История — таблица ниже.

## Что проверено

> Предмет — код плагина (python), не конфигурация 1С: детерминированные
> слои (checkbsl_scan/bsl_ls_analyze/metadata_scan) к `.py` неприменимы —
> их место заняли прогоны ниже. Для BSL/XML-ресурса проверка вливания
> выполнена на живом дереве toir2 (критерий 1 из 04).

| Секция / инструмент | Результат |
|---|---|
| Петля самоочистки (аналог слоёв): полный сьют unittest | ✅ 285 OK (+17: слияние трёх слоёв с приоритетом, CLI-флаги, дифф только-.mdo без Java, подавление metadata-находки, кэш hit/инвалидация/битый, счётчик, битый XML) |
| Живой прогон обёртки на диффе toir2 (`--diff HEAD --src-root --slim-config --cache-dir`) | ✅ одна команда → 39 находок (🔴3/🟡13/🟢23) трёх слоёв; шапка «+ слой метаданных (metadata_scan.py, слито)»; metadata-находки с меткой и карточками; отчёт в toir2 (правило двух отчётов) |
| Дедуп/приоритет/подавления (критерии 2, 8) | ✅ unit: тройная коллизия погашена (выживает metadata); suppress.json глушит metadata-находку со счётчиком |
| Кэш (критерий 7) | ✅ unit: hit/инвалидация mtime/битый файл; замер toir2: standalone 9,7 → 0,57 с, инкремент обёртки +0,8 с; полный тёплый 3,3 с — отступление зафиксировано (позиция внимания 1) |
| Валидаторы плагина | ✅ validate_skills 0 WARN; check_version_sync 0.33.0; `checkbsl_scan.py` не изменялся (git diff); drift-замки зелёные |
| Счётчик покрытия (критерий 4) | ✅ 149/322 = 46,3%, metadata 18/32; README/ARCHITECTURE/докстринг — тест-замок синхронизации |
| High-risk §8 чек-листа (адаптация: инъекции в CLI, надёжность) | ✅ subprocess только git (аргументы списком); кэш — не источник истины (битый → обход); ошибки слоя → exit 2 с диагностикой |

## История раундов

| Раунд | Дата | Решение | Режим | Замечаний |
|---|---|---|---|---|
| 1 | 2026-09-05 | Одобрено | manual | 0 (4 позиции внимания — закрыты решением) |

---
*«Одобрено» — переход на этап «Внешняя приёмка» (`1c-external-acceptance`). До 0.23.0 этапа «Код ревью» не было: самопрогон `1c-code-review` фиксировался строкой «Входные проверки разработки» в шапке 05 (гейт 4→5) — решением Ревьюера он не считается, задачам без 05a `delivery_tools.py` даёт WARN без блокировки.*
