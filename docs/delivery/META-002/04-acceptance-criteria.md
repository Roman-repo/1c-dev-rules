# Критерии приёмки — META-002

**Источники:** критерии успеха из `01-task-brief.md`; шаги из
`02-execution-scenario.md`; реализация из `03-change-spec.md`.

## Матрица трассировки и критерии приёмки

| № | Критерий (проверяемо) | Шаг сценария | Объект/код (03) | Проверка (ручная/unit/BDD) | Статус 05 | Статус 06 |
|---|---|---|---|---|---|---|
| 1 | Дифф с `.bsl` И `.mdo` одним прогоном обёртки → один отчёт `bsl-ls-r<N>.md`; находки всех трёх слоёв; у каждой metadata-находки метка слоя (`ls_code` = «слой метаданных (metadata_scan)») и карточка каталога для каталогных ключей | 2–5 | `run_metadata` + слияние + `build_report` | unit: фикстура-дифф двух типов; живой прогон на toir2 (`--diff HEAD --src-root Project/Toir/src`) — полигон TASK-002 (менеджер/форма/ОМ + `.mdo` объекта) | ✅ | ☐ |
| 2 | Дедуп и подавления действуют на metadata-слой: дубли (тот же ключ/файл/строка) сняты; `--suppress` глушит metadata-находку, счётчик и секция «Подавленные» её показывают | 4–5 | слияние + `apply_suppressions` | unit: коллизия трёх слоёв — одна находка; suppress.json на metadata-ключ — находка скрыта, счётчик = 1 | ✅ | ☐ |
| 3 | Отключение флагом как у слоя 1: `--no-merge-metadata` — слой не запускается; `--merge-metadata FILE` принимает готовый json `metadata_scan.py` | 2 | CLI обёртки | unit: CLI-тесты по образцу `--no-merge-scan`/`--merge-scan` | ✅ | ☐ |
| 4 | Счётчик покрытия отражает metadata-ключи: `--coverage-report` — combined 135 → **149/322 = 46,3%**, секция metadata 4/32 → 18/32 (базлайн «до»: счётчик main 2026-09-04); README/ARCHITECTURE/докстринг синхронизированы | 2 (`--coverage-report`) | `coverage_stats` + тест-замок | unit: числа воспроизводятся из кода; тест синхронизации README/докстринга обновлён и зелёный; вывод закоммичен в 05 | ✅ | ☐ |
| 5 | [Откл 2а] Дифф только с `.mdo`/`.rights`: metadata-находки в общем отчёте, java-прогон не выполняется, exit 0/1 по 🔴 metadata | 2а | дифф-ветка main | unit: tmp git-репо, дифф без `.bsl` | ✅с | ☐ |
| 6 | [Откл 3а] Нет Java/jar: exit 3, сообщение называет ОБА standalone-слоя (`checkbsl_scan.py`, `metadata_scan.py`) | 3а | exit-3 ветка | unit: подмена путей поиска java/jar | ✅с | ☐ |
| 7 | [Откл 4а] Кэш индекса: повторный прогон обёртки с `--cache-dir` без правок входов ≤2 с на дереве toir2; кэш-хит не читает содержимое `.mdo`/`.form`/`.rights`; правка дерева (mtime) инвалидирует кэш; битый кэш → обход заново | 4а, 7а | `Context(cache_dir=…)` | unit: hit/miss/инвалидация/битый файл; замер времени живого прогона — фиксация числа в 05 (базлайн: 0,3 с — кэш BSL LS, PR #71; 9,7 с — metadata-обход без кэша, 05 META-001, 2026-09-04) | ✅с | ☐ |
| 8 | [Откл 5а] Коллизия (ключ, файл, строка) между слоями гасится приоритетом metadata → слой 1 → BSL LS | 5а | обобщённое слияние | unit: три синтетические находки на одном ключе — выживает metadata; коллизия слой1/BSL LS — слой 1 | ✅с | ☐ |
| 9 | [Откл 6а] Битый XML: 🟢 `LocalBrokenXml` в общем отчёте обёртки, остальные источники проверяются | 6а | парсер слоя | unit: фикстура malformed `.mdo` в диффе | ✅с | ☐ |
| 10 | Регрессия: standalone-CLI `metadata_scan.py` без `--cache-dir` — поведение прежнее (замер на полигоне META-001); `checkbsl_scan.py` не менялся; drift-замки целы; полный сьют зелёный (268 + новые) | — (инженерное ограничение 03, НФТ «Регрессия») | tests/ | unit: `python3 -m unittest discover tests`; `check_bsl_ls_drift.py` без сети | ✅с | ☐ |

### Алфавит статусов (обеих колонок)

| Пометка | Значение | Кто ставит |
|---|---|---|
| ☐ | не проверялся | — (начальное) |
| ✅ | проверен живым прогоном, соответствует | 05, 06 |
| ✅с | проверен статически, без живой базы (evidence в 05); интерактивная проверка — на внешней приёмке | 05 |
| ⏳ | статически не проверяем — передан на внешнюю приёмку (05); не проверен, приёмка отложена (06) | 05, 06 |
| ❌ | не соответствует: красная строка, самовозврат класс A (05); замечание приёмщика (06) | 05, 06 |
| n/a | неприменим (строка исключена возвратом класса B) — с пояснением в истории изменений | 05/06 |

Правила заполнения:

- «Шаг сценария» — номер шага из 02 (счастливый путь или отклонение).
- «Проверка» — конкретный способ: «unit: ТестИмя», «ручная, шаг N».
- Сравнительные критерии — ссылка на базлайн «до» (дата, значение, источник):
  критерий 4 — счётчик main 2026-09-04 (135/322, metadata 4/32); критерий 7 —
  PR #71 (0,3 с) и 05 META-001 (9,7 с, 2026-09-04).
- Критерий успеха карточки (01) без критерия приёмки — 🔴 запрещено (DoD).
- Источник: критерий из 01 — без пометки; из отклонения 02 — «[Откл]».

## Gherkin (для BDD-проверок)

Не требуется: предмет — CLI-слой плагина; все критерии закрываются
unit-тестами и живым прогоном на репозитории-полигоне (без интерактивного
UI 1С).

## Порядок заполнения статусов

- **Внутренняя приёмка** (`05-internal-acceptance.md`) — заполняет «Статус 05»
  по всем строкам с evidence; ❌ — самовозврат в разработку (класс A).
- **Внешняя приёмка** (`06-acceptance-protocol.md`) — заполняет «Статус 06»,
  подтверждая (✅) или оспаривая (❌) статус 05; при «Отложено» — ⏳.
- Итоговый статус критерия — колонка «Статус 06»; пока пуста — «Статус 05».

## История изменений

| Дата | Что изменилось | Почему (пункт листа возврата) |
|---|---|---|
| 2026-09-04 | Создана матрица: 4 критерия из 01 + 6 из отклонений 02 | Этап 2 конвейера META-002 |
| 2026-09-05 | Критерий 7, числовая цель «повторный прогон обёртки ≤2 с» уточнена: цель относилась к ИНКРЕМЕНТУ metadata-слоя, а не к полному времени обёртки — базлайн цели был неверен (тёплый прогон обёртки без metadata-слоя на дереве toir2 сам ≈2,5 с: загрузка кэшированного отчёта BSL LS 95,7 МБ ≈1,1 с + импорты). Проверяемая формулировка: инкремент metadata-слоя ≤1 с и standalone-повтор ≤1 с (замер 05: +0,8 с и 0,57 с); полный тёплый прогон 3,3 с против ~10 с до задачи — вынос на Ревьюера (05a) с кандидатом оптимизации (производный кэш по диффу) | Замер 05 вскрыл неверный базлайн цели; уточнение по прецеденту META-001 (история 04, критерий 1); механика кэша (hit/инвалидация/битый файл) подтверждена тестами без изменений |

---
*После подготовки 02/03/04 — этап «Согласование» конвейера: решение
Оркестратора в `04a-design-review.md` (см. `1c-delivery-gate` §0), затем —
этап «Разработка» (`1c-dispatch-gate`).*
