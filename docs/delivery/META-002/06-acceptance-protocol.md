# Протокол приёмки — META-002

- **Дата приёмки:** 2026-09-05
- **Приёмщик:** Roman (сопровождающий плагина; инициатор — трек развития промпта 0.32.0, миссия п.11)
- **Демонстрировал:** Исполнитель (ИИ, ZCode) — свалка 05 + лист ревью 05a («Одобрено», manual, раунд 1) + живой прогон обёртки на дереве toir2; интерактивные команды для самостоятельного повтора — в комментариях
- **Основание:** `04-acceptance-criteria.md`, отчёт `05-internal-acceptance.md`, лист ревью `05a-code-review.md`
- **Предыдущий протокол:** нет

## Результаты по критериям приёмки (из 04)

| № критерия | Критерий | Статус внутренней (05) | Статус приёмщика | Комментарий |
|---|---|---|---|---|
| 1 | Дифф с .bsl И .mdo одним прогоном → один отчёт, метка слоя | ✅ | ✅ | Повтор: `python3 scripts/bsl_ls_analyze.py --diff HEAD --src-root Project/Toir/src --slim-config --cache-dir <dir> --report …` в toir2 — 39 находок трёх слоёв (🔴3/🟡13/🟢23), шапка «+ слой метаданных» (05, критерий 1; отчёт — toir2/docs/code-review/2026-09-05-meta002-merged-layers/) |
| 2 | Дедуп и подавления действуют на metadata-слой | ✅ | ✅ | unit: тройная коллизия погашена, suppress.json глушит metadata-находку со счётчиком |
| 3 | `--no-merge-metadata` / `--merge-metadata FILE` | ✅ | ✅ | unit: CLI-тесты по образцу слоя 1 |
| 4 | Счётчик: combined 149/322 = 46,3%, metadata 18/32; README/ARCH синхронны | ✅ | ✅ | Повтор: `python3 scripts/bsl_ls_analyze.py --coverage-report`; замок-тест в сьюте |
| 5 | [Откл 2а] Дифф только .mdo: находки в отчёте, Java не запускается | ✅с | ✅ | unit (tmp git-репо, фиктивные --java/--jar не задеты) |
| 6 | [Откл 3а] Нет Java: exit 3, оба standalone-слоя в подсказке | ✅с | ✅ | unit |
| 7 | Кэш индекса: инкремент ≤1 с, standalone ≤1 с; механика hit/инвалидация/битый | ✅с | ✅ | Замер toir2: +0,8 с в обёртке, 0,57 с standalone (было 9,7 с обхода); полное время 3,3 с — отступление от исходной «≤2 с» (неверный базлайн) — одобрено Ревьюером в 05a, уточнение в истории 04 |
| 8 | [Откл 5а] Коллизия: приоритет metadata → слой 1 → BSL LS | ✅с | ✅ | unit |
| 9 | [Откл 6а] Битый XML: 🟢 LocalBrokenXml в общем отчёте | ✅с | ✅ | unit (через обёртку) |
| 10 | Регрессия: сьют зелёный, checkbsl_scan не менялся, drift-замки целы | ✅с | ✅ | `python3 -m unittest discover tests` — 285 OK; git diff — checkbsl_scan.py отсутствует |

## Замечания

| № | Замечание (цитата приёмщика) | Класс A/B/C/D | Целевой этап | Вынесено в задачу (для C) |
|---|---|---|---|---|
| | | | | |

<Класс C — новая задача: в лист возврата НЕ включается. Возврат ≠ расширение рамок.

Наблюдения — кандидаты в бэклог (не возвраты): производный кэш находок по
диффу (тёплый прогон обёртки 3,3 с, из них 1,1 с — загрузка кэша отчёта BSL LS
95,7 МБ) — отдельная задача оптимизации петли; далее эпика — META-003 (права
существующих объектов). Всё в 05 «Наблюдения».>

## Решение

- [x] **Принято** — блокирующих замечаний нет
- [ ] **Принято с замечаниями** — только класс D, не блокирующие
- [ ] **Возврат** — лист возврата 06a создан
- [ ] **Отложено** — приёмщик не может выполнить проверку (нет среды/живой
  базы): конвейер стоит до возобновления; переход на «Релиз» запрещён
  (DoD 7→8 требует «принято»); возобновление — новый протокол `.r1`

**Дата пересмотра (обязательна для «Отложено»):** —

**Подписи:** приёмщик Roman 2026-09-05 (решение «Принято» в сессии ZCode), сдавал Исполнитель (ИИ) 2026-09-05
