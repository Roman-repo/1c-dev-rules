# Отчёт внутренней приёмки — META-001

**Дата:** 2026-09-04 · **Задача:** metadata-слой `scripts/metadata_scan.py` (MVP эпика EP-001)

**Самопрогоны разработки (шапка, гейт 4→5):**

- `python3 -m unittest discover` — **268 тестов OK** (было 216; +52 metadata_scan)
- `python3 scripts/validate_skills.py` — PASS, 0 WARN
- `python3 scripts/check_version_sync.py` — 0.32.0 синхронно (plugin/marketplace/kimi/README)
- Детерминированные слои код-ревью (checkbsl_scan/bsl_ls_analyze) к коду плагина неприменимы (.py); для BSL-целей слоя — см. критерий 1

## Прогон критериев (матрица 04)

### Критерий 1 — находки несут ключ + № + «что не так / как правильно»

- **Unit:** 52 теста `tests/test_metadata_scan.py` — пары позитив/негатив на каждую проверку (16 каталогных + 15 локальных), включая sev-понижения (X8 без Экспорт → 🟡) и gates (HAS_RIGHTS/HAS_UI, Report-исключение X5).
- **Живой прогон** (`toir2`, `--diff HEAD --src-root Project/Toir/src`, ветка задачи): 10 находок (🔴2/🟡0/🟢8) на 6 объектах диффа (торо_ОбъектыРемонта, торо_ЗаявкаНаРемонт, Configuration.mdo, 3 подсистемы, Rights.rights). Каждая находка: ключ (каталогный/локальный), № стандарта где есть, «Что не так», «Как правильно» из fixes-базы (каталогные), ссылка на карточку docs.checkbsl.org.
- 🔴 обе — `QueryParamMismatch` на вендорских модулях (менеджер ставит параметры ГР/ДатаПланированияНачало/СсылкаТиповойОбъект/Валюта, запросов с такими &X в модулях объекта нет; в форме — `&РГ` ≠ «ГР»). Проверено вручную grep: истинные по контракту проверки; старый bash-X5 на том же объединении модулей даёт тот же вердикт (паритет). Класс подавления «не баг» — через `--suppress`.

### Критерий 2 — покрытие metadata-ключей ≥15/32

- `python3 scripts/metadata_scan.py --coverage-report`: **16/32** каталогных metadata-ключей (базлайн «до» — 4/32, карточка 01, 2026-09-04). Знаменатель воспроизводится парсингу `references/checkbsl/metadata.md` (не хардкод).
- Замок в тесте: ≥15 + каждый каталогный ключ существует в каталоге и имеет fixes-запись (why+good) — `test_coverage_report_lock`.

### Критерий 3 — CLI/выход совместимы

- Unit: exit 0 (чисто) / 1 (🔴) / 2 (нет входа, путь не найден); `--format md|json`; json-схема findings с catalog-ссылками; `--report` пишет файл; `--diff` без metadata-источников → сообщение + exit 0; подавления `--suppress` — находка скрыта, счётчик «подавлено: 1».

### Критерий 4 — validate-new-object.sh не дублирует слой

- Скрипт перестроен: Часть 1 — вызов `metadata_scan.py` (`--src-root`), Часть 2 — формовые цепочки (F3/F4/F10, X1–X4, X15) + X5/X11 по модулям форм.
- **Живой прогон** на `торо_ИдентификаторыТехКарт`: слой PASS, формовые — 29 PASS / 2 FAIL (X15: СОЕДИНЕНИЕ с СрезПоследних в ДС ФормВыбора/ФормСписка — вендорский код, прежний класс находок 7-го полевого теста, поведение сохранено). Расхождений по перенесённым проверкам нет: исполнитель проверок один (слой), bash только вызывает.

### Критерии 5–8 (отклонения 02)

- 5 (битый XML): unit `test_broken_xml_warns_not_crashes` — 🟢 WARN, остальные файлы проверяются, exit 0 без 🔴.
- 6 (нераспознанный тип): unit `test_unknown_type_lists_skipped` — LocalTypeUnknown с перечнем пропущенного (живой прогон: подсистемы в диффе — честное 🟢).
- 7 (подавления): unit `test_suppress_hides_but_counts` (счётчик, не молчание).
- 8 (дифф без XML): unit `test_diff_without_mdo_sources` (tmp git-репо).

### Критерий 9 — регрессия плагина

- Сьют 268 — OK; validate_skills 0 WARN; version sync OK.
- ⚠️ Отступление от границ 03 (зафиксировано в истории 03): точечный фикс `checkbsl_scan.diff_files` — `git -c core.quotepath=off` (тот же класс бага: без него дифф-режим ОБОИХ сканеров молча терял кириллические пути — норма для 1С; воспроизведено на toir2: `--diff HEAD` возвращал 1 файл вместо 7). Вынесено в PR на решение Ревьюера.

## НФТ (замеры, 03)

| Требование | Факт | Вывод |
|---|---|---|
| Дифф-режим <1 с | без кросс-файловых индексов <0,3 с; с полным обходом ≈9,7 с | см. ниже |
| Проектный обход (UUID, Roles/) <5 с | **9,7 с** на дереве toir2: 500 МБ / 5257 файлов .mdo+.form+.rights, один проход (слияние трёх обходов в один) | НФТ 03 оптимистична: базлайн «bash rule12 ≈2,6 с» на полном объёме не воспроизводится (объём выгрузки недооценён). 60 МБ/с — пол/потолок чтения. Кандидат оптимизации — кэш индекса по mtime (контур META-002, по аналогии с `--cache-dir` BSL LS); для петли ревью 10 с приемлемо (полный прогон BSL LS на той же конфигурации — 67 с) |
| Совместимость: python ≥3.9, только stdlib, без Java | да (argparse/json/pathlib/xml.etree/re) | ✅ |
| Формат EDT | живое дерево toir2 (новый формат mdclass-namespace + старые Rights.rights) | ✅; смена формата — правка по факту (риск 01) |

## Наблюдения живого прогона (кандидаты в плагин, вне рамок задачи)

1. `core.quotepath` — см. критерий 9 (закрыто в этом PR).
2. `MetadataSynonym` (🟢) срабатывает на большинстве вендорских объектов (префикс торо_/гкс_ + транслитерация EWO/ЕВО) — эвристика по назначению рекомендательная; при шуме на реальных диффах — кандидат уточнения (учёт префикса проекта из AGENTS.md).
3. X5 на объектах с СКД-шаблонами non-Report (торо_ОбъектыРемонта): параметры могут относиться к запросам схем в `Templates/*.dcs`, которые X5 не читает (читал ли bash — нет) — кандидат расширения X5/X14 на СКД-шаблоны документов/справочников (META-00X).

## Вердикт внутренней приёмки

Готово к код ревью (этап 6): все 9 критериев проверены (4 — живым прогоном на дереве toir2, 5 — unit/статически), блокирующих красных строк нет. Отступление от границ 03 (quotepatch checkbsl_scan) и отклонение НФТ-тайминга зафиксированы и вынесены на Ревьюера.
