# Протокол приёмки — META-001

- **Дата приёмки:** 2026-09-04
- **Приёмщик:** Roman (инициатор — диагностика анализатора 2026-08-31)
- **Демонстрировал:** Исполнитель (ИИ, ZCode) — свалка 05 + живые прогоны на дереве toir2; интерактивные команды для самостоятельного повтора — в комментариях
- **Основание:** `04-acceptance-criteria.md`, отчёт `05-internal-acceptance.md`, лист ревью `05a-code-review.md` («Одобрено»)
- **Предыдущий протокол:** нет

## Результаты по критериям приёмки (из 04)

| № критерия | Критерий | Статус внутренней (05) | Статус приёмщика | Комментарий |
|---|---|---|---|---|
| 1 | Находки слоя несут ключ + № стандарта + «что не так» + «как правильно» | ✅ | ✅ | Повтор: `python3 scripts/metadata_scan.py --diff HEAD --src-root Project/Toir/src` в toir2 — 10 находок, обе 🔴 истинные (05, критерий 1) |
| 2 | Каталогные metadata-ключи: ≥15/32 (базлайн 4/32) | ✅ | ✅ | Повтор: `python3 scripts/metadata_scan.py --coverage-report` → 16/32 |
| 3 | CLI/выход совместимы (md/json, exit 0/1/2, --diff/--src-root/--suppress/--report) | ✅ | ☐ | Unit-прогон: `python3 -m unittest tests.test_metadata_scan` |
| 4 | validate-new-object.sh не дублирует слой (вызывает; свои — формовые) | ✅ | ✅ | Повтор: `bash scripts/validate-new-object.sh Project/Toir/src/Catalogs/торо_ИдентификаторыТехКарт` в toir2 |
| 5 | [Откл] Битый XML → WARN, остальные файлы проверяются | ✅с | ✅ | unit test_broken_xml_warns_not_crashes |
| 6 | [Откл] Нераспознанный тип → явный перечень пропущенного | ✅с | ✅ | unit + живой прогон (подсистемы в диффе) |
| 7 | [Откл] Подавленная находка видна счётчиком | ✅с | ✅ | unit test_suppress_hides_but_counts |
| 8 | [Откл] Дифф без XML-источников → сообщение, exit 0 | ✅с | ✅ | unit test_diff_without_mdo_sources |
| 9 | Регрессия: сьют зелёный, слои не изменялись* | ✅с | ☐ | `python3 -m unittest discover` — 268 OK; *checkbsl_scan — только quotepath-фикс (05, критерий 9; одобрено в 05a) |

## Замечания

| № | Замечание (цитата приёмщика) | Класс A/B/C/D | Целевой этап | Вынесено в задачу (для C) |
|---|---|---|---|---|
| | | | | |

<Класс C — новая задача: в лист возврата НЕ включается. Возврат ≠ расширение рамок.

Наблюдения живого прогона — кандидаты в бэклог эпика (не возвраты): кэш
индекса при тайминге >5 с, уточнение MetadataSynonym по префиксам проекта,
X5 на СКД-шаблонах non-Report — всё в 05 «Наблюдения», адресат META-002+.>

## Решение

- [x] **Принято** — блокирующих замечаний нет
- [ ] **Принято с замечаниями** — только класс D, не блокирующие
- [ ] **Возврат** — лист возврата 06a создан
- [ ] **Отложено** — приёмщик не может выполнить проверку (нет среды/живой
  базы): конвейер стоит до возобновления; переход на «Релиз» запрещён
  (DoD 7→8 требует «принято»); возобновление — новый протокол `.r1`

**Дата пересмотра (обязательна для «Отложено»):** —

**Подписи:** приёмщик Roman 2026-09-04 (решение «Принято» в сессии ZCode), сдавал Исполнитель (ИИ) 2026-09-04
