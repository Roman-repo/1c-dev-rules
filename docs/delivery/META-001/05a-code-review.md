# Лист код-ревью — META-001

## Решение

- [x] **Одобрено** — код прошёл ревью, переход на «Внешнюю приёмку» (`1c-external-acceptance`)
- [ ] **Доработать** — замечания в таблице ниже; возврат на «Разработку», правка, повторное ревью (этот же файл)

**Режим ревью:** manual
**Ревьюер:** Roman — решение по PR #89 в сессии ZCode (дифф PR + сводка внутренней приёмки 05)
**Дата:** 2026-09-04

## Замечания

| № | Замечание | Правило/Стандарт | Место | Статус |
|---|---|---|---|---|
| — | Блокирующих замечаний нет. Позиции внимания, принятые Ревьюером с решением «Одобрено» (из 05): (1) отступление от границ 03 — точечный фикс `checkbsl_scan.diff_files` (`git -c core.quotepath=off`, дифф терял кириллические пути); (2) НФТ-тайминг: полный обход toir2 ≈9,7 с при цели <5 с (объём выгрузки 500 МБ; кэш-кандидат — META-002); (3) `WrongSynonym` сужен до цифры в начале (буквенный регистр по каталогу противоречит №474 — ложняк на каждый объект); (4) `MetadataSynonym` (🟢) шумит на вендорских префиксах — кандидат уточнения по живым диффам | 03 (границы), НФТ | scripts/checkbsl_scan.py:diff_files; scripts/metadata_scan.py | ✅ |

Правила:

- «Доработать» без строки замечания — бессмысленно: исправлять нечего.
  Каждое замечание — про конкретное место кода (файл:строка) и конкретное
  правило (ключ из каталога `references/checkbsl/` или № стандарта — как в
  чек-листе скила).
- Ревью проверяет **качество кода по стандартам**, не требования: спор о
  требованиях — не замечание ревью, а возврат с внешней приёмки по классам
  A/B (`1c-delivery-gate` §3).
- Замечания по правилам с важностью 🔴 (чек-лист, каталог checkbsl) —
  блокирующие: «Одобрено» при открытых 🔴-замечаниях не ставится.
- После правки Ревьюер повторно принимает решение этим же файлом: статусы
  замечаний → ✅, решение обновляется. История — таблица ниже.

## Что проверено

> Предмет — код плагина (python/bash), не конфигурация 1С: детерминированные
> слои (checkbsl_scan/bsl_ls_analyze) к `.py` неприменимы — их место заняли
> прогоны ниже. Для BSL-части ресурса проверка слоем выполнена на живом
> дереве toir2 (критерий 1 из 04).

| Секция / инструмент | Результат |
|---|---|
| Петля самоочистки (аналог слоёв): полный сьют unittest | ✅ 268 OK (+52 metadata_scan: пары позитив/негатив на каждую проверку, CLI, подавления, замок покрытия) |
| Живой прогон слоя на диффе toir2 (`--diff HEAD --src-root`) | ✅ 10 находок (🔴2/🟡0/🟢8), обе 🔴 истинные (QueryParamMismatch вендорских модулей, подтверждено grep) |
| Живой прогон перестроенного validate-new-object.sh | ✅ слой PASS + формовые 29 PASS / 2 FAIL (вендорские X15, прежний класс) — паритет перенесённых проверок |
| Валидаторы плагина | ✅ validate_skills 0 WARN; check_version_sync 0.32.0 |
| Раздел каталога checkbsl metadata (применимость ключей) | ✅ 16 ключей слоя существуют в каталоге, fixes у каждого (замок-тест) |
| High-risk §8 чек-листа (адаптация: инъекции в CLI, надёжность) | ✅ exit-коды изолированы, битые входы → WARN/exit 2, subprocess только git (аргументы списком) |

## История раундов

| Раунд | Дата | Решение | Режим | Замечаний |
|---|---|---|---|---|
| 1 | 2026-09-04 | Одобрено | manual | 0 (4 позиции внимания — закрыты решением) |

---
*«Одобрено» — переход на этап «Внешняя приёмка» (`1c-external-acceptance`). До 0.23.0 этапа «Код ревью» не было: самопрогон `1c-code-review` фиксировался строкой «Входные проверки разработки» в шапке 05 (гейт 4→5) — решением Ревьюера он не считается, задачам без 05a `delivery_tools.py` даёт WARN без блокировки.*
