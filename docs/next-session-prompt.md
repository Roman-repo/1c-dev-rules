# Промпт для переезда в новую сессию — 6-е полевое тестирование плагина 1c-dev-rules

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, цель, список кейсов, процедура, стиль, формат отчёта.

---

Контекст: ШЕСТОЕ полевое тестирование плагина 1c-dev-rules на реальных задачах 1С.
Продолжаем развивать репозиторий правил.

Рабочий каталог: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР, EDT/БСП, платформа **8.3.27**
— подняли в 3-й сессии с 8.3.12, ~2822 BSL-модуля). Исходники: Project/Toir/src/.
Проект-донор для проверки плагина вживую.

ПЛАГИН: ~/Projects/1c-dev-rules (репозиторий-источник: github.com/Roman-repo/1c-dev-rules).
Версия на main: **0.8.0** (все PR смёржены: #1–#20; функциональные #1–#11, #16–#20;
docs: #12–#15). Плагин теперь **двухплатформенный**: ZCode (`.zcode-plugin/plugin.json` +
`marketplace.json`) и Kimi Work (`kimi.plugin.json`, личный маркет).

ПРОМЕЖУТОЧНАЯ СЕССИЯ «K» (между 5-й и 6-й полевой, НЕ полевая — toir2 не трогали):
- **Kimi-совместимость** (PR #16–#17, 0.5.7→0.5.9): `kimi.plugin.json` (skills — МАССИВ
  22→23 путей, строка-путь даёт 1 скил!); триггеры `when_to_use` продублированы в
  `description` (роутер Kimi читает только description); `bump-version.sh` бампит
  все ТРИ манифеста атомарно; README «Вариант 4 — Kimi».
- **Аудит содержимого** (PR #18–#20, 0.5.10→0.8.0): 4 пробела закрыты —
  D1 диагностика проде-инцидентов (`1c-performance/references/diagnostics.md`: ТЖ,
  замеры, сценарии deadlock/блокировок); D3 аутентификация в API
  (`1c-integration/references/integration-auth.md`: OAuth 2.0, секреты в безопасном
  хранилище); D4 обмен (`references/exchange-edm.md`: EnterpriseData/КД/планы обмена);
  D2 командная разработка (23-й скил `1c-collab`: хранилище/EDT+git, обновление БСП,
  чек-лист релиза). Попутно починены: шаблоны `HTTPСоединение` (таймаут стоял на
  позиции прокси!), битые ссылки `std/index.md` (28 шт).

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ, до всего остального):
1. В СВОЁМ system-reminder проверь наличие **23 скилов** 1c-* (включая `1c-collab`)
   и сверь версию в путях. Нужно 0.8.0. Если кэш застрял на старой — это ОЖИДАЕМо.
   Примени УРОК ДОСТАВКИ (ниже), затем перепроверь. (Фолбэк работает: чтение свежего
   контента из ~/Projects/1c-dev-rules/skills/... и прогон скрипта из исходника.)
2. Маркеры 0.8.0 в клоне/исходнике (после обновления):
   • `kimi.plugin.json` — существует, `skills` — массив из 23 путей, версия 0.8.0;
   • `skills/1c-collab/SKILL.md` — существует (хранилище/EDT+git/релизы);
   • `skills/1c-performance/references/diagnostics.md` — техжурнал, `logcfg.xml`;
   • `skills/1c-integration/references/integration-auth.md` и `exchange-edm.md`;
   • `1c-integration/SKILL.md` — шаблон `HTTPСоединение(..., 80, , , , 30)` (4 запятые,
     таймаут на 6-й позиции);
   • `scripts/bump-version.sh` — упоминает `kimi.plugin.json` (бампит 3 манифеста);
   • плюс маркеры 0.5.7: `validate-new-object.sh` — case распознаёт Reports (13 типов),
     «X13», «X12», «HAS_UI», «TYPE_UNKNOWN», «os.walk», «X8»/«X11»;
     `skills/1c-thin-triggers/SKILL.md` — «Создал подписку на событие», «отчёт на СКД»;
     `skills/1c-metadata/references/metadata-xml.md` §7.1 — таблица X1–X13.
3. 🔴 УРОК ДОСТАВКИ — ТЕПЕРЬ ДВА КАНАЛА:
   • **ZCode** (подтверждён в 3–5-й сессиях): для git-source маркетплейса НИ рестарт,
     НИ reinstall НЕ делают `git pull` клона. Чини так:
     `git -C ~/.zcode/cli/plugins/marketplaces/1c-dev-rules fetch origin main`
     затем `git -C ... merge --ff-only origin/main`, ПЕРЕЗАПУСК клиента, reinstall;
     либо пере-добавить маркетплейс через Settings → Plugin Management → Discover → «+».
   • **Kimi Work** (подтверждено в сессии K): нет git-клона. Копия плагина лежит в
     `~/Documents/kimi/workspace/1c-dev-rules` — синхронизировать из репо
     (`rsync -a --delete --exclude=.git`), затем перерегистрация в личном маркете
     (PersonalPluginRegister) и установка/обновление со страницы плагинов, вкладка
     «Личное». Регистрация только валидирует + записывает; установка — вручную в UI.
   Без этого скилы останутся старой версии; самопроверку кейсов можно вести по свежему
   main репозитория плагина (Read из ~/Projects/1c-dev-rules/skills/...).

ЧТО УЖЕ СДЕЛАНО В ПРЕДЫДУЩИХ СЕССИЯХ (НЕ повторяй — двигай дальше):
• Сессия 1: обработка торо_ВыгрузкаНоменклатурыВJSON → PR #1, 0.3.0→0.4.0.
  Добавлено: кросс-файловая §7 в 1c-thin-triggers + metadata-xml.md; validate-new-object.sh.
• Сессия 2: баланс-регистр торо_РезервЗапчастейНаРемонт + торо_КорректировкаРезерваЗапчастей
  с проведением по остаткам → PR #2, 0.4.0→0.5.0 (F1–F9).
• Сессия 3: prep + кейсы A (РЗ) / C (HTTP-сервис) / D (CFE) → PR #3–#6, 0.5.0→0.5.4 (G1–G7', X11).
• Сессия 4: кейсы G (подписки) / F (безопасность) → PR #8–#9, 0.5.4→0.5.6.
• Сессия 5: кейс B (отчёт на СКД) → PR #11, 0.5.6→0.5.7.
• Сессия K (не полевая): Kimi-совместимость + аудит содержимого → PR #16–#20, 0.5.7→0.8.0.
  См. блок выше; toir2 не изменялся.

  УЖЕ починено в сессиях 1–5 (НЕ переоткрывай):
   - F1–F9, G1–G7', X11, G-EVT-1..4, F-G2, F-SEC-1 (см. CHANGELOG.md).
   - **B-1 (PR#11, кейс B): validate распознаёт Reports (Report); автоматически работают
     X6/X7 (№532 — Use/View)/X7b (HAS_UI=1); X13 — mainDataCompositionSchema → шаблон
     объявлен в <templates> (templateType=DataCompositionSchema) → Templates/<Имя>/Template.dcs
     существует. Опечатка/отсутствие схемы → FAIL.**
   - **B-4 (PR#11): X5 ложно FAIL-ил на СКД-отчётах — параметры инлайн-запроса в ObjectModule
     связаны через КомпоновщикНастроек, а не УстановитьПараметр. Для Report модули
     объекта/менеджера из X5 исключены.**
   - **B-2/B-5 (PR#11): thin-triggers — строки кросс-файловой согласованности для отчётов СКД;
     metadata-xml §7.1 — таблица цепочек дополнена с X1–X7 до X1–X13 (ранее таблица отставала
     от скрипта, не показывая X8/X9/X11/X12).**

  ОТЛОЖЕНО (можно подобрать, пакет P4 аудита):
   - B-3 (🟢 пробел): автоматическая проверка DCS &параметр ↔ <parameter> в схеме (X14 —
     аналог X5 для СКД). Сейчас задокументировано в thin-triggers (ручной grep), но НЕ
     автоматизировано в validate. СКД-параметры проверяет только X13 (схема↔Template),
     согласованность имён параметров внутри схемы — вручную.
   - №630 (🟢 пробел): validate-new-object.sh НЕ проверяет формально наличие 5 обязательных
     областей модуля формы (только companion-элементы F3/F4 и цепочки X1–X4). Проверить
     в кейсе E — если подтвердится, добавить проверку в скрипт.

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — постоянная инструкция пользователя:
   ВСЕ правки плагина — обязательно коммитить+push; ВСЕ правки объектов ТОИР — БЕЗ коммитов):
  ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. В рабочем дереве лежат
  правки ВСЕХ сессий 1–5 (последние коммиты toir2 — docs:, прикладной код незакоммичен):
   -prep: Configuration.mdo (compatibilityMode 8.3.27), AGENTS.md (правило 11);
   - Сессия 1: DataProcessors/торо_ВыгрузкаНоменклатурыВJSON, features/;
   - Сессия 2: AccumulationRegisters/торо_РезервЗапчастейНаРемонт, Documents/торо_КорректировкаРезерваЗапчастей, Roles/;
   - Сессия 3: CommonModules/торо_НапоминанияОПлановыхРемонтрах, ScheduledJobs/*,
     InformationRegisters/торо_ОтправленныеНапоминанияОППР, HTTPServices/торо_ОстаткиРезерваЗапчастей,
     Extensions/торо_ДоработкаАктаРабот, Roles/торо_БазовыеПрава;
   - Сессия 4: CommonModules/торо_АктВыполненияРаботСервер (.mdo+Module.bsl — обработчик
     валидации дат), EventSubscriptions/торо_ПроверитьДатыРемонтовПередЗаписью (.mdo),
     + регистрация в Configuration.mdo; HTTPServices/торо_ОстаткиРезерваЗапчастей/Module.bsl
     усилен (№678 валидация repair_id + №737 ПравоДоступа 403).
   - **Сессия 5: Reports/торо_РасходЗапчастейПоОбъектамРемонта (.mdo + ObjectModule.bsl +
     Templates/ОсновнаяСхемаКомпоновкиДанных/Template.dcs — 3-пакетный запрос СКД),
     Roles/торо_ОтчетРасходЗапчастейПоОбъектамРемонта (.mdo+Rights.rights),
     + регистрация в Configuration.mdo (reports+roles) и подсистеме торо_УчетОборудованияИНормативов.**
  ВНИМАНИЕ: метаданные кейсов 3-й (HTTPService, CFE), 4-й (подписка+ОМ) и 5-й сессии
  (Report: .mdo + DCS-схема) созданы без EDT-валидатора (правило 13) — структура требует
  проверки при импорте в EDT.

ЦЕЛЬ ЭТОЙ СЕССИИ — продолжить тестирование на КЛАССАХ, ещё НЕ покрытых, с ОБЯЗАТЕЛЬНЫМ
циклом по каждому кейсу:
  разработка в toir2 по правилам плагина → самопроверка → отчёт об отклонениях →
  фикс в репо плагина → верификация → коммит/PR.
Искать НОВЫЕ отклонения СВЕРХ F1–F9, G1–G7', X11, G-EVT-1..4, F-G2, F-SEC-1, B-1, B-2, B-4, B-5.

СПИСОК КЕЙСОВ (разностороннее покрытие; каждый кейс = полный цикл):

✅ ВЫПОЛНЕНО (НЕ повторять): A — РЗ (сессия 3); B — отчёт на СКД (сессия 5);
   C — HTTP-сервис (сессия 3); D — CFE-перехватчик (сессия 3); G — подписки на события
   (сессия 4); F — безопасность/RLS (сессия 4); + сессии 1–2 (обработка, проведение).
   Контентные пробелы D1–D4 аудита закрыты в сессии K (не путать с кейсом D!).

КЕЙС E — Форма (создание новой / доработка типовой БСП). 🔴 ПРИОРИТЕТ — последний
  непокрытый крупный класс. Почва для 1c-forms: 5 обязательных областей, modifiesStoredData,
  условное оформление вместо кода, плоский динамический список.
  Задача-кандидат: форма списка/элемента справочника торо_ОбъектыРемонта с УО и
  динамическим списком; либо доработка формы документа торо_АктОВыполненииЭтапаРабот.
  Стресс: №629/№630 (области формы), №489/№732 (плоский динамический список), №399
  (условное оформление вместо кода), №703 (запрет модальности), modifiesStoredData.
  Скилы: 1c-forms, 1c-ui-ux, 1c-performance, (если правка XML формы) 1c-metadata + metadata-xml §6.
  Примечание: форма = правка .form XML (вслепую, правило 13). validate-new-object.sh
  проверяет companion-элементы форм (F3/F4) и кросс-файловые цепочки X1–X4 (реквизит↔форма↔
  модуль). Проверь, ловит ли скрипт отсутствие 5 обязательных областей (№630) — это
  отложенный пробел P4 (формальный check по областям сейчас НЕ делается).

КЕЙС E' (дополнительно, если E даст мало находок) — Динамический список / плоский запрос.
  Сложный список с соединениями и обращениями через точку → №732, №655. Либо контекстный
  vs внеконтекстный сервер (№636). Почва для 1c-performance/1c-queries.

ПОРЯДОК ВЫБОРА: по умолчанию предлагай E (единственный непокрытый крупный класс).
При сомнении — через AskUserQuestion. Желательно пройти 1–2 кейса за сессию.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.8.0 — универсальные правила разработки 1С без проектной специфики.
   Главный маршрутизатор: 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.
   Специфика ТОИР: префиксы торо_/гкс_, карта движений (03_Документы_и_движения.md),
   бизнес-логика (торо_Ремонты, торо_РаботаСНаработкой, торо_ПланированиеРемонтов),
   очередь уведомлений (docs/standards-cards/practice/notifications-queue.md).

ПРОЦЕДУРА НА КАЖДЫЙ КЕЙС (после выбора):
1) Маршрутизация: 1c-dispatch-gate (плагин) ОДНОВРЕМЕННО с docs/standards-cards/dispatch-map.md
   (toir2) — пройди оба гейт-чек-листа.
2) Архитектурный чек-лист: docs/standards-cards/arch-checklist.md (7 вопросов) + 1c-arch-checklist.
3) Открой тематические 1c-* скилы + параллельные карточки toir2. Читай текст, не полагайся
   на память.
4) Реализация в Project/Toir/src по правилам обоих слоёв.
5) Самопроверка: 1c-thin-triggers → 1c-code-review (плагин), параллельно
   docs/standards-cards/thin-triggers.md и docs/code-review/ЧекЛистКодРевью.md (toir2).
   🔴 ЕСЛИ создаёшь/правишь метаданные — прогони
   `bash ~/Projects/1c-dev-rules/scripts/validate-new-object.sh <путь-к-объекту>` (dogfooding).
   Скрипт (0.8.0) распознаёт 13 типов (Catalog/Document/DataProcessor/InformationRegister/
   AccumulationRegister/Constant/ChartOfCharacteristicTypes/CommonModule/ScheduledJob/
   HTTPService/WebService/EventSubscription/Report) и проверяет: UUID-дубликаты, синоним ru,
   конфигурационную регистрацию (X6), роли №532 (X7), подсистему (X7b — серьёзность по
   HAS_UI), параметры запроса &X↔УстановитьПараметр (X5, для Report модули объекта/менеджера
   исключены — СКД), handler РЗ methodName↔Module.bsl (X8), Schedule.schedule (X9),
   CFE &ИзменениеИКонтроль↔ПродолжитьВызов (X11), handler подписки↔Module.bsl (X12),
   mainDataCompositionSchema↔Template.dcs (X13). Для нераспознанного типа — предупреждение.
   Если скрипт упал или дал ложное срабатывание — это ТОЖЕ находка для плагина.
6) ОТЧЁТ об отклонениях (формат ниже) — по итогу каждого кейса.
7) 🔴 КОРРЕКТИВЫ В ПЛАГИНЕ: каждое подтверждённое отклонение → правка в ~/Projects/1c-dev-rules.
   • Верифицируй фикс (прогон скрипта на объекте toir2 / перечитка скила / grep-проверка).
   • Версию бампай ЧЕРЕЗ `bash ~/Projects/1c-dev-rules/scripts/bump-version.sh patch|minor`
     (бампит все три манифеста: plugin.json + marketplace.json + kimi.plugin.json).
   • Коммит на новой ветке `feat/...` от main, конвеншен-коммиты, PR в main, мерж через gh
     (squash, по аналогии с PR #1–#20). После мержа — sync клона маркетплейса ZCode
     и/или копии в workspace Kimi (см. УРОК ДОСТАВКИ).

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции. Не-100%-стандартная функция должна быть определена в проекте:
  grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl.
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП (без префикса) — НЕ править.
- Буква ё в именах метаданных ЗАПРЕЩЕНА.
- Новые UUID метаданных — только случайные (uuidgen | tr 'A-Z' 'a-z'), включая
  typeId/valueTypeId в producedTypes; проверка grep -rn "<uuid>" Project/Toir/src.
- 🔴 Правка метаданных XML вслепую (без EDT/MCP) — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА (главный артефакт сессии):
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода>
  Предложение: <что добавить/исправить в плагине>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) Выполни ШАГ 0 — подтверди версию кэша / примени УРОК ДОСТАВКИ до 0.8.0
   (в ZCode — fetch+merge клона; в Kimi — rsync копии в workspace + перерегистрация).
2) Предложи пользователю выбор кейса (E приоритет) через AskUserQuestion.
3) По выбранному кейсу — двойная маршрутизация → разработка → самопроверка → отчёт →
   фикс в плагине → верификация.
4) Повтори для следующего кейса (цель 1–2 за сессию).
5) Финал: приоритезированный список отклонений + РЕАЛЬНО внесённые фиксы + верификация +
   бамп версии + коммит/PR + обновление этого next-session-prompt.md.

ОЖИДАЕМЫЙ ИТОГ СЕССИИ: плагин 0.8.0 → 0.8.1+ (если кейс E/формы даст substantially новый
функционал — minor-бамп до 0.9.0; иначе серия patch); каждый кейс оставляет след и в коде
toir2, и в улучшении плагина. После E крупные непокрытые классы исчерпаны — далее точечные
кейсы (P4: B-3/X14 + №630, производительность, локализация).
