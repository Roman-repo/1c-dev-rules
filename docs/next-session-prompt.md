# Промпт для переезда в новую сессию — после сессии 8 (0.11.0)

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, цель, процедура, стиль, формат отчёта.

---

Контекст: плагин 1c-dev-rules после СЕССИИ 8 — **0.11.0**, **29 скилов**:
23 разработки (кейсы A–G, E', сессии 1–7, полевое тестирование разработки
исчерпано) + **6 скилов слоя ЖИЗНЕННОГО ЦИКЛА ЗАДАЧИ** (новый класс, сессия 8,
PR #26): `1c-delivery-gate`, `1c-planning`, `1c-solution-design`,
`1c-internal-acceptance`, `1c-external-acceptance`, `1c-release`.
Модель слоя: этап → роль участника → обязательные артефакты (01–06a +
_releases/) → DoD-гейт; возвраты A/B/C/D (C = новая задача, не возврат).

Рабочий каталог для прикладных задач: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР,
EDT/БСП, платформа 8.3.27, ~2822 BSL-модуля). Исходники: Project/Toir/src/.
ПЛАГИН: ~/Projects/1c-dev-rules (github.com/Roman-repo/1c-dev-rules). Все PR
смёржены: #1–#26. Двухплатформенный: ZCode + Kimi Work.

СЕССИЯ 8 (слой ЖЦ задачи) — PR #26, 0.10.0→0.11.0:
- **1c-delivery-gate** — маршрутизатор ЖЦ (открывается первым при старте
  ЗАДАЧИ; dispatch-gate остаётся гейтом этапа «Разработка»): карта
  «этап × роль × скил × артефакты», DoD-гейты, матрица ролей, сквозной
  пример; карточки lifecycle-map.md + rework-rules.md (возвраты: жизненный
  цикл, повторная приёмка по листу возврата, сигналы здоровья).
- **1c-planning** (РП) → 01-task-brief.md (проблема→цель-не-решение,
  измеримые критерии успеха, рамки «что НЕ делаем», S/M/L после рамок).
- **1c-solution-design** (архитектор+аналитик) → 02-execution-scenario.md
  (формат: №|Роль|Шаг|Триггер|Подшаги|Результат + обязательные альтернативные
  потоки), 03-change-spec.md (объекты/миграции/права/impact/откат +
  обязательный arch-checklist), 04-acceptance-criteria.md (критерии + матрица
  трассировки без пустых ячеек; мост к Gherkin/Vanessa).
- **1c-internal-acceptance** (разработчик-тестировщик) → 05 (прогон КАЖДОГО
  шага сценария с evidence; красное = самовозврат класса A).
- **1c-external-acceptance** (приёмщик) → 06-протокол + 06a-лист возврата;
  калибровка A vs B («если бы сценарий выполнился буква-в-букву, было бы
  хорошо?»); C = новая задача («возврат ≠ расширение рамок»); D косметика.
- **1c-release** (РП/релиз-инженер) → release-manifest.md + release-notes.md
  (в _releases/<версия>/; только задачи с протоколом «принято»; свод
  миграций из всех 03; техническая сборка осталась в 1c-collab).
- Cross-links: dispatch-gate (секция «Этап 1. Планирование (до кода)» →
  «Подготовка к коду», терминология ЖЦ), testing (источник BDD = Сценарий
  исполнения), collab (технический vs артефактный слой релиза).

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ):
1. В system-reminder проверь **29 скилов** 1c-* (23 старых + 6 новых
   lifecycle) и версию **0.11.0** в путях кэша.
2. Маркеры 0.11.0 в ~/Projects/1c-dev-rules (main, коммит a0282a8 или новее):
   • `skills/1c-delivery-gate/SKILL.md` и ещё 5 новых каталогов скилов;
   • `docs/CHANGELOG.md` — запись [0.11.0] от 2026-08-15;
   • все три манифеста — 0.11.0;
   • `python3 scripts/validate_skills.py` — PASS;
   • README — секция «🚚 Жизненный цикл задачи», бейдж 29.
3. 🔴 УРОК ДОСТАВКИ — ДВА КАНАЛА:
   • **ZCode**: клон маркетплейса
     ~/.zcode/cli/plugins/marketplaces/1c-dev-rules УЖЕ синхронизирован до
     a0282a8 (сессия 8). Осталось: ПЕРЕЗАПУСК клиента + reinstall плагина
     (если кэш ещё 0.10.0). git pull не нужен, если log = a0282a8+.
   • **Kimi Work**: копия в ~/Documents/kimi/workspace/1c-dev-rules.
     ⚠️ rsync из терминала снова упёрся в macOS TCC (Operation not permitted,
     сессия 8) — копия ОСТАЛАСЬ НА 0.10.0. Дать терминалу Full Disk Access
     (System Settings → Privacy & Security → Full Disk Access) либо руками:
     `rsync -a --delete --exclude=.git ~/Projects/1c-dev-rules/
     ~/Documents/kimi/workspace/1c-dev-rules/`, затем перерегистрация
     (PersonalPluginRegister) и обновление в UI (вкладка «Личное»).
   Без этого скилы останутся старыми; самопроверку можно вести по свежему main
   (Read из ~/Projects/1c-dev-rules/skills/...).

ЧТО УЖЕ СДЕЛАНО (НЕ повторяй):
• Сессии 1–7: прикладные классы (JSON, проведение, РЗ, HTTP, CFE, подписки,
  безопасность, СКД, формы, ДС) + скрипт X1–X15/F10 — полевое тестирование
  разработки исчерпано. Починено: F1–F9, G1–G7', X11, G-EVT-1..4, F-G2,
  F-SEC-1, B-1/2/4/5, E-1…E-5, E'-1…E'-3, B-3/X14. См. CHANGELOG.md.
• Сессия 8: слой ЖЦ задачи — 6 скилов + cross-links + README/ARCHITECTURE
  (PR #26). Полевая обкатка слоя ЖЦ НЕ проводилась.

ЦЕЛЬ СЛЕДУЮЩЕЙ СЕССИИ — по выбору пользователя:
  a) ПОЛЕВАЯ ОБКАТКА СЛОЯ ЖЦ (по умолчанию): одна реальная задача в toir2
     от постановки до релиза по скилам 1c-delivery-gate → 1c-planning → … →
     1c-release с созданием артефактов 01–06 в docs/delivery/<ID>/; ловить
     отклонения/неполноту шаблонов — это их первое реальное применение;
  b) эксплуатация разработки (обычные задачи по слою разработки);
  c) полировка слоя ЖЦ по находкам обкатки.
  Через AskUserQuestion предложи варианты; по умолчанию — (a).

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — постоянная инструкция: правки плагина —
  обязательно commit+push; правки объектов ТОИР — БЕЗ коммитов):
  ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. В рабочем дереве
  правки сессий 1–7 (сессия 8 ТОИР НЕ трогала). ФормаМониторинга: queryText ДС
  + 2 колонки (id 53–58) — проверить при импорте в EDT (правка вслепую,
  правило 13). Новое: артефакты ЖЦ будущих задач — docs/delivery/ в toir2.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.11.0 — универсальные правила + слой ЖЦ. Маршрутизаторы:
   задача → 1c-delivery-gate; работа по коду → 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.

ПРОЦЕДУРА НА ЗАДАЧУ (слой ЖЦ):
1) Маршрутизация: 1c-delivery-gate — определи этап, войди в роль, открой
   скил этапа текстом.
2) Каждый этап завершай артефактом по шаблону из references/ скила
   (docs/delivery/<ID>/ в toir2; на первом применении каталог создать).
3) Этап «Разработка» — по старой процедуре (dispatch-gate + arch-checklist +
   тематические + thin-triggers/code-review + validate-new-object.sh).
4) DoD-гейт delivery-gate §2 — до перехода; возвраты — §3 + rework-rules.md.
5) Отчёт об отклонениях (формат ниже).

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции: не-100%-стандартная должна быть определена в проекте
  (grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl).
  `СообщитьПользователю` — функция БСП (ОбщегоНазначенияКлиентСервер).
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП — НЕ править. Буква ё в именах
  метаданных ЗАПРЕЩЕНА. Новые UUID — только случайные (uuidgen).
- 🔴 Правка метаданных XML вслепую — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА (главный артефакт; относится теперь
и к скилам ЖЦ — шаблоны/DoD/классификация при первом реальном применении):
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода/артефакта>
  Предложение: <что добавить/исправить>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) ШАГ 0 — верификация версии/доставки до 0.11.0.
2) AskUserQuestion: цель сессии (a — полевая обкатка слоя ЖЦ по умолчанию /
   b — эксплуатация разработки / c — полировка по находкам).
3) Выполняй выбранное по процедуре; отклонения — отчёт + фикс + PR
   (bump-version.sh patch → 0.11.x).

ОЖИДАЕМЫЙ ИТОГ: слой ЖЦ обкатан на реальной задаче (артефакты 01–06 в toir2);
точечные PR по находкам (0.11.x — patch). Новые классы — minor (0.12+).
