# Промпт для переезда в новую сессию — ЗАКРЫТИЕ ПРИЁМКИ TASK-001 (0.11.2)

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, миссия, процедура, стиль, формат отчёта.

---

Контекст: плагин 1c-dev-rules **0.11.2** (ветка fix/field-run-task001…, PR #30),
**29 скилов** = 23 разработки + 6 конвейера разработки (Планирование →
Проектирование → Разработка → Внутренняя приёмка → Внешняя приёмка → Релиз).
Конвейер прошёл первый полевой прогон (сессия 9, задача TASK-001) —
**до конца**: внешняя приёмка ОТЛОЖЕНА (нет живой базы), релиз — черновик.

Рабочий каталог для прикладных задач: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР,
EDT/БСП, платформа 8.3.27). Исходники: Project/Toir/src/.
ПЛАГИН: ~/Projects/1c-dev-rules (github.com/Roman-repo/1c-dev-rules).
Двухплатформенный: ZCode + Kimi Work.

ИСТОРИЯ (кратко; НЕ повторяй):
• Сессии 1–8: прикладные классы + скрипт X1–X15/F10 + слой конвейера (0.11.0),
  терминология «конвейер разработки» (0.11.1). См. CHANGELOG.md.
• Сессия 9 (PR #30, 0.11.2): полевой прогон конвейера на TASK-001 — компактная
  печатная форма «Заявка на ремонт (согласование)» для торо_ЗаявкаНаРемонт.
  Полный цикл: 01-brief…06-protocol + _releases/2.0.27.0-draft/. Закрыто 7
  находок (X5 &&Маркер; статусы «отложено»/«статич.»/draft; [АП]; mxlx-карточка;
  №436-исключение). Отчёт: docs/field-reports/2026-08-15-conveyor-run-task001.md.

СОСТОЯНИЕ TASK-001 (каталог ~/Projects/toir2/docs/delivery/TASK-001/):
• Код и макет НАПИСАНЫ вслепую (XML без EDT): ManagerModule.bsl
  (ПечатьСогласования/ВывестиПустоеСогласование/ИтогиСогласования + команда
  «Согласование»), Templates/ПФ_MXL_Согласование/Template.mxlx, регистрация в
  mdo (uuid 40c7c0b1-…). Существующая «Смета ремонта» не тронута (дифф пробельный).
• Внутренняя приёмка 05 — статическая, все строки ✅ (статич.); интерактивные — ⏳.
• Внешняя приёмка 06 — «отложено»: приёмщик без живой базы.
• Релиз _releases/2.0.27.0-draft/ — черновик, «не включять до приёмки».

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ):
1. PR #30 смёржен? git log main на GitHub. Если да — версия 0.11.2 в манифестах,
   CHANGELOG [0.11.2], docs/field-reports/…, README без изменений слоя.
   Если нет — спроси пользователя про план мержа (сам не мержи без указания).
2. Доставка (🔴 два канала):
   • ZCode: клон маркетплейса ~/.zcode/cli/plugins/marketplaces/1c-dev-rules —
     git pull до мержа #30; после мержа: ПЕРЕЗАПУСК клиента + reinstall плагина,
     если кэш < 0.11.2.
   • Kimi Work: копия в ~/Documents/kimi/workspace/1c-dev-rules осталась на
     0.10.0 (TCC: Operation not permitted, сессии 7–9). Дать терминалу Full Disk
     Access либо вручную: rsync -a --delete --exclude=.git
     ~/Projects/1c-dev-rules/ ~/Documents/kimi/workspace/1c-dev-rules/,
     затем PersonalPluginRegister + обновление в UI.
3. system-reminder: 29 скилов 1c-*, версия в путях кэша.

МИССИЯ СЕССИИ — ЗАКРЫТЬ КОНВЕЙЕР TASK-001 (живой прогон):
1) Пользователь импортирует toir2 в EDT (правки XML вслепую — обязательная
   проверка структуры макета: индексы форматов/шрифтов, области) и запускает базу.
2) Внешняя приёмка по матрице 04 (критерии 1–7): меню Печать → вторая команда;
   поля сверкой; итоги vs смета; пустая заявка; нет цен. Результат → новый
   протокол 06-acceptance-protocol.r1.md (ссылка на «отложенный»).
3) Возвраты по классам A/B/D → 06a-rework-list → правка → повторный прогон
   затронутых строк (правила rework-rules). Класс C — новая задача.
4) После «принято»: повысить черновик релиза _releases/2.0.27.0-draft/ до
   боевого (убрать -draft, заполнить «Отбор» протоколом .r1).
5) Отклонения плагина, всплывшие на живом прогоне — отчёт (формат ниже) →
   фикс → PR → bump-version.sh patch (0.11.2.x).

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — правки объектов ТОИР и АРТЕФАКТЫ ЗАДАЧ — БЕЗ
коммитов; правки плагина — commit+push): ~/Projects/toir2, ветка
feat/refactor-gant-split-procedure. Правки сессий 1–9: ФормаМониторинга (ДС,
колонки 53–58), менеджер/макет/mdo ЗаявкиНаРемонт, docs/delivery/.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.11.2. Маршрутизаторы: задача → 1c-delivery-gate; код внутри
   этапа «Разработка» → 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции: не-100%-стандартная должна быть определена в проекте
  (grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl).
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП — НЕ править. Буква ё в именах
  метаданных ЗАПРЕЩЕНА. Новые UUID — только случайные (uuidgen).
- 🔴 Правка метаданных XML вслепую — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА:
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода/артефакта>
  Предложение: <что добавить/исправить>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) ШАГ 0 — верификация мержа #30 и доставки 0.11.2.
2) Спроси пользователя: импортирован ли toir2 в EDT, готова ли живая база.
3) Если да — веди внешнюю приёмку по миссии выше; если нет — предложи помочь
   с импортом/запуском (диагностика EDT вне досягаемости — тогда фиксируй
   следующую паузу и спроси, чем занять сессию: патчи из черновых заметок
   отчёта 2026-08-15: code-review «ВТ удаляются», шаблон 05 под статич./⏳).

ОЖИДАЕМЫЙ ИТОГ: 06-acceptance-protocol.r1 = «принято» (или возврат с классами и
цикл правки); релиз повышен из черновика; отклонения живого прогона — отчёт+PR.
После закрытия TASK-001 — обновить этот промпт (docs PR): следующая миссия —
второй полевой прогон конвейера на независимой задаче (M-класс).
