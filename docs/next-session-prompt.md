# Промпт для переезда в новую сессию — 7-е полевое тестирование плагина 1c-dev-rules

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, цель, список кейсов, процедура, стиль, формат отчёта.

---

Контекст: СЕДЬМОЕ полевое тестирование плагина 1c-dev-rules на реальных задачах 1С.
Продолжаем развивать репозиторий правил.

Рабочий каталог: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР, EDT/БСП, платформа **8.3.27**
— подняли в 3-й сессии с 8.3.12, ~2822 BSL-модуля). Исходники: Project/Toir/src/.
Проект-донор для проверки плагина вживую.

ПЛАГИН: ~/Projects/1c-dev-rules (репозиторий-источник: github.com/Roman-repo/1c-dev-rules).
Версия на main: **0.9.0** (все PR смёржены: #1–#22; функциональные #1–#11, #16–#20, #22;
docs: #12–#15, #21). Плагин **двухплатформенный**: ZCode (`.zcode-plugin/plugin.json` +
`marketplace.json`) и Kimi Work (`kimi.plugin.json`, личный маркет).

СЕССИЯ 6 (кейс E — формы, последний крупный непокрытый класс) — PR #22, 0.8.0→0.9.0:
- **Кейс E**: новая форма `ФормаМониторинга` справочника торо_ОбъектыРемонта в toir2 —
  плоский ДС (поля только справочника, без точек/ВТ), программное УО в ПриСозданииНаСервере
  (серый для исключённых из планирования, фон для неактивных), быстрый отбор по
  подразделению (элемент отбора создан один раз на сервере, клиент меняет Значение/
  Использование), 2 команды с `modifiesStoredData=true`, серверная часть —
  `&НаСервереБезКонтекста` (№636) → метод менеджера `УстановитьУчастиеВПланировании`
  (идемпотентность, `Заблокировать()` перед Записать, ЖР по №499/790, `ОповеститьОбИзменении`).
- **Скрипт**: реализованы X2/X3/X4 (заявлены в §7.1 с 0.4.0, но НЕ проверялись):
  X2 dataPath↔attributes, X3 commandName→formCommands→handler (включает F7),
  X4 handlers↔Module.bsl — Python-проходка на форму; F10 — области модуля формы №630
  (закрыт отложенный пробел P4). Негативная верификация пройдена.
- **Скилы**: 1c-ui-ux/1c-forms — образец `СообщитьПользователю` исправлен на
  `ОбщегоНазначенияКлиентСервер.СообщитьПользователю` (это функция БСП, НЕ платформенная —
  образец провоцировал галлюцинацию API); снято противоречие «5 vs 4 областей»
  (typical-forms-new говорил «5, даже пустые»); таблица ДС = таблица формы
  (`ОбработчикиСобытийЭлементовТаблицыФормыСписок`).
- **Попутные находки на типовом коде вендора** (истинные, НЕ фиксы плагина):
  ФормаЭлемента — команда `ОткрытьДокументВводаНачальныхДанных` без action (кнопка
  disabled), обработчик `АдресМестонахожденияОткрытие` без процедуры в модуле;
  ManagerModule — мёртвые `УстановитьПараметр` (ГР/СсылкаТиповойОбъект/ДатаПланированияНачало,
  X5 FAIL на объекте). НЕ переоткрывать — это код поставщика.

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ, до всего остального):
1. В СВОЁМ system-reminder проверь наличие **23 скилов** 1c-* (включая `1c-collab`)
   и сверь версию в путях. Нужно 0.9.0. Если кэш застрял на старой — это ОЖИДАЕМо.
   Примени УРОК ДОСТАВКИ (ниже), затем перепроверь. (Фолбэк работает: чтение свежего
   контента из ~/Projects/1c-dev-rules/skills/... и прогон скрипта из исходника.)
2. Маркеры 0.9.0 в клоне/исходнике (после обновления):
   • `scripts/validate-new-object.sh` — содержит «X2/X3/X4», «F10», «№630»,
     «ОбработчикиСобытийЭлементовТаблицыФормыСписок» (в комментарии), python-блок
     с `formCommands` и `commandName`;
   • `skills/1c-ui-ux/SKILL.md` — «ОбщегоНазначенияКлиентСервер.СообщитьПользователю»;
   • `skills/1c-metadata/references/metadata-xml.md` §6.1 — строка F10; §7.2 —
     «X2/X3/X4 для форм»;
   • `docs/CHANGELOG.md` — запись [0.9.0] от 2026-08-14;
   • все три манифеста (plugin.json / marketplace.json / kimi.plugin.json) — 0.9.0;
   • плюс маркеры 0.8.0: `skills/1c-collab/SKILL.md`,
     `1c-performance/references/diagnostics.md`, `1c-integration/references/
     integration-auth.md` и `exchange-edm.md`, `kimi.plugin.json` (23 skills).
3. 🔴 УРОК ДОСТАВКИ — ТЕПЕРЬ ДВА КАНАЛА:
   • **ZCode** (подтверждён 3–6-й сессиях): для git-source маркетплейса НИ рестарт,
     НИ reinstall НЕ делают `git pull` клона. Чини так:
     `git -C ~/.zcode/cli/plugins/marketplaces/1c-dev-rules fetch origin main`
     затем `git -C ... merge --ff-only origin/main`, ПЕРЕЗАПУСК клиента, reinstall;
     либо пере-добавить маркетплейс через Settings → Plugin Management → Discover → «+».
   • **Kimi Work** (подтверждено в сессии K): нет git-клона. Копия плагина лежит в
     `~/Documents/kimi/workspace/1c-dev-rules` — синхронизировать из репо
     (`rsync -a --delete --exclude=.git`), затем перерегистрация в личном маркете
     (PersonalPluginRegister) и установка/обновление со страницы плагинов, вкладка
     «Личное». Регистрация только валидирует + записывает; установка — вручную в UI.
   Без этого скилы останутся старой версии; самопроверку кейсов можно вести по свежему
   main репозитория плагина (Read из ~/Projects/1c-dev-rules/skills/...).

ЧТО УЖЕ СДЕЛАНО В ПРЕДЫДУЩИХ СЕССИЯХ (НЕ повторяй — двигай дальше):
• Сессия 1: обработка торо_ВыгрузкаНоменклатурыВJSON → PR #1, 0.3.0→0.4.0.
• Сессия 2: баланс-регистр торо_РезервЗапчастейНаРемонт + торо_КорректировкаРезерваЗапчастей
  с проведением по остаткам → PR #2, 0.4.0→0.5.0 (F1–F9).
• Сессия 3: prep + кейсы A (РЗ) / C (HTTP-сервис) / D (CFE) → PR #3–#6, 0.5.0→0.5.4 (G1–G7', X11).
• Сессия 4: кейсы G (подписки) / F (безопасность) → PR #8–#9, 0.5.4→0.5.6.
• Сессия 5: кейс B (отчёт на СКД) → PR #11, 0.5.6→0.5.7 (B-1/2/4/5).
• Сессия K (не полевая): Kimi-совместимость + аудит содержимого → PR #16–#20, 0.5.7→0.8.0.
• Сессия 6: кейс E (форма списка) → PR #22, 0.8.0→0.9.0 (X2/X3/X4, F10/№630,
  СообщитьПользователю-БСП, 4/5 областей, ДС-таблица).

  УЖЕ починено (НЕ переоткрывай): F1–F9, G1–G7', X11, G-EVT-1..4, F-G2, F-SEC-1,
  B-1, B-2, B-4, B-5, E-1 (№630→F10), E-2 (X2/X3/X4), E-3 (4/5 областей),
  E-4 (ДС-таблица), E-5 (СообщитьПользователю). См. CHANGELOG.md.

  ОТЛОЖЕНО (пакет P4 — остатки):
   - B-3 (🟢 пробел): автоматическая проверка DCS &параметр ↔ <parameter> в схеме
     (X14 — аналог X5 для СКД). Задокументировано в thin-triggers (ручной grep),
     НЕ автоматизировано в validate.
   - E' (кейс, не пробел): динамический список под нагрузкой — соединения и обращения
     через точку (№732, №655), контекстный vs внеконтекстный сервер (№636 — частично
     закрыт в сессии 6). Почва для 1c-performance/1c-queries.

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — постоянная инструкция пользователя:
   ВСЕ правки плагина — обязательно коммитить+push; ВСЕ правки объектов ТОИР — БЕЗ коммитов):
  ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. В рабочем дереве лежат
  правки ВСЕХ сессий 1–6 (последние коммиты toir2 — docs:, прикладной код незакоммичен):
   -prep: Configuration.mdo (compatibilityMode 8.3.27), AGENTS.md (правило 11);
   - Сессия 1: DataProcessors/торо_ВыгрузкаНоменклатурыВJSON, features/;
   - Сессия 2: AccumulationRegisters/торо_РезервЗапчастейНаРемонт, Documents/торо_КорректировкаРезерваЗапчастей, Roles/;
   - Сессия 3: CommonModules/торо_НапоминанияОПлановыхРемонтрах, ScheduledJobs/*,
     InformationRegisters/торо_ОтправленныеНапоминанияОППР, HTTPServices/торо_ОстаткиРезерваЗапчастей,
     Extensions/торо_ДоработкаАктаРабот, Roles/торо_БазовыеПрава;
   - Сессия 4: CommonModules/торо_АктВыполненияРаботСервер, EventSubscriptions/торо_ПроверитьДатыРемонтовПередЗаписью,
     + регистрация в Configuration.mdo; HTTPServices/торо_ОстаткиРезерваЗапчастей усилен.
   - Сессия 5: Reports/торо_РасходЗапчастейПоОбъектамРемонта (+DCS-схема), Roles/торо_ОтчетРасходЗапчастейПоОбъектамРемонта,
     + регистрация в Configuration.mdo и подсистеме.
   - **Сессия 6: Catalogs/торо_ОбъектыРемонта: Forms/ФормаМониторинга/Form.form + Module.bsl
     (новые файлы), ManagerModule.bsl (+Функция УстановитьУчастиеВПланировании в области
     ПрограммныйИнтерфейс), торо_ОбъектыРемонта.mdo (+forms-блок ФормаМониторинга, uuid
     a92c11b5-c8df-4826-87c6-d349cbf4d9af).**
  ВНИМАНИЕ: метаданные кейсов 3–6 сессий созданы без EDT-валидатора (правило 13) —
  структура требует проверки при импорте в EDT. Форма мониторинга: УО программное
  (обоснование в комментарии модуля), автообновление ДС не включено.

ЦЕЛЬ ЭТОЙ СЕССИИ — добить остатки покрытия и собрать урожай точечных кейсов,
  с ОБЯЗАТЕЛЬНЫМ циклом по каждому кейсу:
  разработка в toir2 по правилам плагина → самопроверка → отчёт об отклонениях →
  фикс в репо плагина → верификация → коммит/PR.
  Искать НОВЫЕ отклонения СВЕРХ перечисленных выше.

СПИСОК КЕЙСОВ (каждый кейс = полный цикл):

✅ ВЫПОЛНЕНО (НЕ повторять): A — РЗ; B — отчёт на СКД; C — HTTP-сервис; D — CFE;
   G — подписки; F — безопасность/RLS; E — форма списка (сессия 6); + сессии 1–2
   (обработка, проведение). Крупные классы исчерпаны.

КЕЙС E' — Динамический список / плоский запрос под нагрузкой. 🔴 ПРИОРИТЕТ.
  Сложный список с соединениями и обращениями через точку → №732, №655. Либо
  контекстный vs внеконтекстный сервер (№636 — углубить: сессия 6 закрыла один вызов).
  Кандидат: доработка ФормыМониторинга (сессия 6) — добавить в ДС соединение
  (например, гкс_КатегорияРиска через явное ЛЕВОЕ СОЕДИНЕНИЕ vs через точку) и
  замерить/обосновать; либо ревью существующих тяжёлых списков toir2
  (ФормаЭлемента торо_ОбъектыРемонта — 284 сегмента dataPath!) на №732.
  Скилы: 1c-performance, 1c-queries, 1c-forms.

КЕЙС P4 (точечный, без полевой разработки) — B-3/X14: автоматизация проверки
  DCS &параметр ↔ <parameter> в validate-new-object.sh (аналог X5 для СКД-схем).
  Есть готовый полигон: Reports/торо_РасходЗапчастейПоОбъектамРемонта (сессия 5).
  Если E' даст мало находок — закрыть X14.

ПОРЯДОК ВЫБОРА: по умолчанию предлагай E' (P4 — запасной). При сомнении — через
AskUserQuestion. Желательно пройти 1–2 кейса за сессию.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.9.0 — универсальные правила разработки 1С без проектной специфики.
   Главный маршрутизатор: 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.
   Специфика ТОИР: префиксы торо_/гкс_, карта движений (03_Документы_и_движения.md),
   бизнес-логика (торо_Ремонты, торо_РаботаСНаработкой, торо_ПланированиеРемонтов),
   очередь уведомлений (docs/standards-cards/practice/notifications-queue.md).

ПРОЦЕДУРА НА КАЖДЫЙ КЕЙС (после выбора):
1) Маршрутизация: 1c-dispatch-gate (плагин) ОДНОВРЕМЕННО с docs/standards-cards/dispatch-map.md
   (toir2) — пройди оба гейт-чек-листа.
2) Архитектурный чек-лист: docs/standards-cards/arch-checklist.md (7 вопросов) + 1c-arch-checklist.
3) Открой тематические 1c-* скилы + параллельные карточки toir2. Читай текст, не полагайся
   на память.
4) Реализация в Project/Toir/src по правилам обоих слоёв.
5) Самопроверка: 1c-thin-triggers → 1c-code-review (плагин), параллельно
   docs/standards-cards/thin-triggers.md и docs/code-review/ЧекЛистКодРевью.md (toir2).
   🔴 ЕСЛИ создаёшь/правишь метаданные — прогони
   `bash ~/Projects/1c-dev-rules/scripts/validate-new-object.sh <путь-к-объекту>` (dogfooding).
   Скрипт (0.9.0) распознаёт 13 типов и проверяет: UUID-дубликаты, синоним ru,
   конфигурационную регистрацию (X6), роли №532 (X7), подсистему (X7b), параметры
   запроса &X↔УстановитьПараметр (X5, Report-модули исключены), handler РЗ (X8),
   Schedule (X9), CFE (X11), handler подписки (X12), схему СКД (X13) + для форм:
   dataPath↔attributes (X2), команды (X3), обработчики↔Module.bsl (X4), области
   модуля формы №630 (F10). Если скрипт упал или дал ложное срабатывание — это ТОЖЕ
   находка для плагина.
6) ОТЧЁТ об отклонениях (формат ниже) — по итогу каждого кейса.
7) 🔴 КОРРЕКТИВЫ В ПЛАГИНЕ: каждое подтверждённое отклонение → правка в ~/Projects/1c-dev-rules.
   • Верифицируй фикс (прогон скрипта на объекте toir2 / перечитка скила / grep-проверка).
   • Версию бампай ЧЕРЕЗ `bash ~/Projects/1c-dev-rules/scripts/bump-version.sh patch|minor`
     (бампит все три манифеста: plugin.json + marketplace.json + kimi.plugin.json).
   • Коммит на новой ветке `feat/...` от main, конвеншен-коммиты, PR в main, мерж через gh
     (squash, по аналогии с PR #1–#22). После мержа — sync клона маркетплейса ZCode
     и/или копии в workspace Kimi (см. УРОК ДОСТАВКИ).

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции. Не-100%-стандартная функция должна быть определена в проекте:
  grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl.
  🔴 Особый случай: `СообщитьПользователю` — функция БСП
  (`ОбщегоНазначенияКлиентСервер.СообщитьПользователю`), НЕ платформенная (фикс 0.9.0).
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП (без префикса) — НЕ править.
- Буква ё в именах метаданных ЗАПРЕЩЕНА.
- Новые UUID метаданных — только случайные (uuidgen | tr 'A-Z' 'a-z'), включая
  typeId/valueTypeId в producedTypes; проверка grep -rn "<новый uuid>" Project/Toir/src.
- 🔴 Правка метаданных XML вслепую (без EDT/MCP) — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА (главный артефакт сессии):
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода>
  Предложение: <что добавить/исправить в плагине>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) Выполни ШАГ 0 — подтверди версию кэша / примени УРОК ДОСТАВКИ до 0.9.0
   (в ZCode — fetch+merge клона; в Kimi — rsync копии в workspace + перерегистрация).
2) Предложи пользователю выбор кейса (E' приоритет, P4/X14 запасной) через AskUserQuestion.
3) По выбранному кейсу — двойная маршрутизация → разработка → самопроверка → отчёт →
   фикс в плагине → верификация.
4) Повтори для следующего кейса (цель 1–2 за сессию).
5) Финал: приоритезированный список отклонений + РЕАЛЬНО внесённые фиксы + верификация +
   бамп версии + коммит/PR + обновление этого next-session-prompt.md.

ОЖИДАЕМЫЙ ИТОГ СЕССИИ: плагин 0.9.0 → 0.9.1+ (X14/B-3 — patch: точечная проверка в скрипт;
E' — patch/minor по находкам). После E' и X14 полевое тестирование можно считать
исчерпанным: все крупные классы + все известные пробелы закрыты — дальше только
эксплуатация и новые классы задач по мере появления.
