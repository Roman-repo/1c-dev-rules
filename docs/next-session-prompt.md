# Промпт для переезда в новую сессию — 5-е полевое тестирование плагина 1c-dev-rules

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, цель, список кейсов, процедура, стиль, формат отчёта.

---

Контекст: ПЯТОЕ полевое тестирование плагина 1c-dev-rules на реальных задачах 1С.
Продолжаем развивать репозиторий правил.

Рабочий каталог: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР, EDT/БСП, платформа **8.3.27**
— подняли в 3-й сессии с 8.3.12, ~2822 BSL-модуля). Исходники: Project/Toir/src/.
Проект-донор для проверки плагина вживую.

ПЛАГИН: ~/Projects/1c-dev-rules (репозиторий-источник: github.com/Roman-repo/1c-dev-rules).
Версия на main: **0.5.6** (все 9 PR смёржены: #1–#9). Кэш ZCode, скорее всего, ещё
0.5.0 — см. ШАГ 0 (УРОК ДОСТАВКИ).

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ, до всего остального):
1. В СВОЁМ system-reminder проверь наличие 22 скилов 1c-* и сверь версию в путях.
   Нужно 0.5.6. Если кэш застрял на 0.5.0 — это ОЖИДАЕМо. Примени УРОК ДОСТАВКИ,
   затем перепроверь. (Фолбэк на 4-й сессии работал: чтение свежего контента из
   ~/Projects/1c-dev-rules/skills/... и прогон скрипта из исходника.)
2. Маркеры 0.5.6 в клоне/исходнике (после обновления):
   • scripts/validate-new-object.sh — case распознаёт EventSubscriptions (12 типов);
     содержит «X12» (handler подписки↔Module.bsl), «HAS_UI» (X7b серьёзность по типу),
     «TYPE_UNKNOWN» (предупреждение о нераспознанном типе), «os.walk» (правило 12),
     «X8»/«X11»;
   • skills/1c-thin-triggers/SKILL.md — строки «Создал подписку на событие» (handler↔Module.bsl)
     и «серверного API» (HTTP/WS-обработчик → №678/№737);
   • skills/1c-metadata/references/metadata-xml.md §7.2 — «X1–X12» + список типов;
   • scripts/bump-version.sh — существует.
3. 🔴 УРОК ДОСТАВКИ (подтверждён в 3-й и 4-й сессиях): для git-source маркетплейса
   НИ рестарт, НИ reinstall НЕ делают `git pull` клона. Чини так:
   • `git -C ~/.zcode/cli/plugins/marketplaces/1c-dev-rules fetch origin main`
     затем `git -C ... merge --ff-only origin/main`, ПЕРЕЗАПУСК клиента, reinstall;
   • либо пере-добавить маркетплейс через Settings → Plugin Management → Discover → «+».
   Без этого скилы в ZCode останутся старой версии; самопроверку кейсов можно вести
   по свежему main репозитория плагина (Read из ~/Projects/1c-dev-rules/skills/...).

ЧТО УЖЕ СДЕЛАНО В ПРЕДЫДУЩИХ СЕССИЯХ (НЕ повторяй — двигай дальше):
• Сессия 1: обработка торо_ВыгрузкаНоменклатурыВJSON → PR #1, 0.3.0→0.4.0.
  Добавлено: кросс-файловая §7 в 1c-thin-triggers + metadata-xml.md; validate-new-object.sh.
• Сессия 2: баланс-регистр торо_РезервЗапчастейНаРемонт + торо_КорректировкаРезерваЗапчастей
  с проведением по остаткам → PR #2, 0.4.0→0.5.0 (F1–F9).
• Сессия 3: prep + кейсы A (РЗ) / C (HTTP-сервис) / D (CFE) → PR #3–#6, 0.5.0→0.5.4 (G1–G7', X11).
• Сессия 4 (предыдущая): кейсы G (подписки) / F (безопасность) → PR #8–#9, 0.5.4→0.5.6.

  УЖЕ починено в сессиях 1–4 (НЕ переоткрывай):
   - F1–F9, G1–G7', X11 (см. промпт 4-й сессии / CHANGELOG.md).
   - G-EVT-1..4 (PR#8, кейс G): validate распознаёт EventSubscriptions; X12
     (handler↔Module.bsl — зеркало X8); TYPE_UNKNOWN — предупреждение вместо ложного
     PASS для нераспознанного типа; thin-triggers — строка handler-цепочки подписки;
     metadata-xml §7.2 — диапазон X1–X12 + список типов.
   - F-G2 (PR#9, кейс F): X7b серьёзность по типу — HAS_UI; для не-UI типов (регистры,
     HTTP/Web-сервисы, константы) отсутствие подсистемы → INFO (было ложный WARN).
   - F-SEC-1 (PR#9): thin-triggers — триггер для серверного API (HTTP/WS-обработчик,
     экспорт с «Вызов сервера» → №678 валидация входа + №737 ПравоДоступа).

  ОТЛОЖЕНО (можно подобрать):
   - (нет крупных; мелочи могут всплыть в новых кейсах).

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — постоянная инструкция пользователя:
   ВСЕ правки плагина — обязательно коммитить+push; ВСЕ правки объектов ТОИР — БЕЗ коммитов):
  ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. В рабочем дереве лежат
  правки ВСЕХ сессий 1–4 (последние коммиты toir2 — docs:, прикладной код незакоммичен):
   -prep: Configuration.mdo (compatibilityMode 8.3.27), AGENTS.md (правило 11);
   - Сессия 1: DataProcessors/торо_ВыгрузкаНоменклатурыВJSON, features/;
   - Сессия 2: AccumulationRegisters/торо_РезервЗапчастейНаРемонт, Documents/торо_КорректировкаРезерваЗапчастей, Roles/;
   - Сессия 3: CommonModules/торо_НапоминанияОПлановыхРемонтрах, ScheduledJobs/*,
     InformationRegisters/торо_ОтправленныеНапоминанияОППР, HTTPServices/торо_ОстаткиРезерваЗапчастей,
     Extensions/торо_ДоработкаАктаРабот, Roles/торо_БазовыеПрава;
   - Сессия 4: CommonModules/торо_АктВыполненияРаботСервер (.mdo+Module.bsl — обработчик
     валидации дат), EventSubscriptions/торо_ПроверитьДатыРемонтовПередЗаписью (.mdo),
     + регистрация в Configuration.mdo; HTTPServices/торо_ОстаткиРезерваЗапчастей/Module.bsl
     усилен (№678 валидация repair_id + №737 ПравоДоступа 403).
  ВНИМАНИЕ: метаданные кейсов 3-й сессии (HTTPService, CFE) и 4-й (подписка+ОМ) созданы
  без EDT-валидатора (правило 13) — структура .mdo требует проверки при импорте в EDT.

ЦЕЛЬ ЭТОЙ СЕССИИ — продолжить тестирование на КЛАССАХ, ещё НЕ покрытых, с ОБЯЗАТЕЛЬНЫМ
циклом по каждому кейсу:
  разработка в toir2 по правилам плагина → самопроверка → отчёт об отклонениях →
  фикс в репо плагина → верификация → коммит/PR.
Искать НОВЫЕ отклонения СВЕРХ F1–F9, G1–G7', X11, G-EVT-1..4, F-G2, F-SEC-1.

СПИСОК КЕЙСОВ (разностороннее покрытие; каждый кейс = полный цикл):

✅ ВЫПОЛНЕНО (НЕ повторять): A — РЗ (сессия 3); C — HTTP-сервис (сессия 3);
   D — CFE-перехватчик (сессия 3); G — подписки на события (сессия 4);
   F — безопасность/RLS (сессия 4); + сессии 1–2 (обработка, проведение).

КЕЙС B — Отчёт на СКД (класс №3). 🔴 ПРИОРИТЕТ — единственный непокрытый из A–D.
  Задача: отчёт «Фактические затраты запчастей по объектам ремонта» на базе
  торо_ФактическиеЗатратыЗапчастей + торо_ОбъектыРемонта, с программным
  ПриКомпоновкеРезультата, параметрами периода, ВТ.
  Стресс: №670 (содержание), №733 (СрезПоследних/Обороты с параметрами), №655 (нет ВТ
  в соединении напрямую), №656 (нет ВЫБРАТЬ *), ПриКомпоновкеРезультата.
  Скилы: 1c-reports, 1c-queries + practice/dcs-design.md.
  Примечание: отчёт = новый объект Report. Проверь, распознаёт ли validate тип Reports
  (в 0.5.6 его НЕТ в case — возможна находка по аналогии с EventSubscriptions).

КЕЙС E — Форма (создание новой / доработка типовой БСП). Новый класс, почва для
  1c-forms: 5 обязательных областей, modifiesStoredData, условное оформление вместо
  кода, плоский динамический список.
  Задача-кандидат: форма списка/элемента справочника торо_ОбъектыРемонта с УО и
  динамическим списком; либо доработка формы документа торо_АктОВыполненииЭтапаРабот.
  Скилы: 1c-forms, 1c-ui-ux, 1c-performance (№489/№732).

ПОРЯДК ВЫБОРА: по умолчанию предлагай B → E (B — приоритет, непокрытый).
При сомнении — через AskUserQuestion. Желательно пройти 1–2 кейса за сессию.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.5.6 — универсальные правила разработки 1С без проектной специфики.
   Главный маршрутизатор: 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.
   Специфика ТОИР: префиксы торо_/гкс_, карта движений (03_Документы_и_движения.md),
   бизнес-логика (торо_Ремонты, торо_РаботаСНаработкой, торо_ПланированиеРемонтов),
   очередь уведомлений (docs/standards-cards/practice/notifications-queue.md).

ПРОЦЕДУРА НА КАЖДЫЙ КЕЙС (после выбора):
1) Маршрутизация: 1c-dispatch-gate (плагин) ОДНОВРЕМЕННО с docs/standards-cards/dispatch-map.md
   (toir2) — пройди оба гейт-чек-листа.
2) Архитектурный чек-лист: docs/standards-cards/arch-checklist.md (7 вопросов) + 1c-arch-checklist.
3) Открой тематические 1c-* скилы + параллельные карточки toir2. Читай текст, не полагайся
   на память.
4) Реализация в Project/Toir/src по правилам обоих слоёв.
5) Самопроверка: 1c-thin-triggers → 1c-code-review (плагин), параллельно
   docs/standards-cards/thin-triggers.md и docs/code-review/ЧекЛистКодРевью.md (toir2).
   🔴 ЕСЛИ создаёшь/правишь метаданные — прогони
   `bash ~/Projects/1c-dev-rules/scripts/validate-new-object.sh <путь-к-объекту>` (dogfooding).
   Скрипт (0.5.6) распознаёт 12 типов (Catalog/Document/DataProcessor/InformationRegister/
   AccumulationRegister/Constant/ChartOfCharacteristicTypes/CommonModule/ScheduledJob/
   HTTPService/WebService/EventSubscription) и проверяет: UUID-дубликаты, синоним ru,
   конфигурационная регистрация (X6), роли №532 (X7), подсистему (X7b — серьёзность по
   HAS_UI), параметры запроса &X↔УстановитьПараметр (X5), handler РЗ methodName↔Module.bsl
   (X8), Schedule.schedule (X9), CFE &ИзменениеИКонтроль↔ПродолжитьВызов (X11),
   handler подписки↔Module.bsl (X12). Для нераспознанного типа — предупреждение.
   Если скрипт упал или дал ложное срабатывание — это ТОЖЕ находка для плагина.
6) ОТЧЁТ об отклонениях (формат ниже) — по итогу каждого кейса.
7) 🔴 КОРРЕКТИВЫ В ПЛАГИНЕ: каждое подтверждённое отклонение → правка в ~/Projects/1c-dev-rules.
   • Верифицируй фикс (прогон скрипта на объекте toir2 / перечитка скила / grep-проверка).
   • Версию бампай ЧЕРЕЗ `bash ~/Projects/1c-dev-rules/scripts/bump-version.sh patch|minor`.
   • Коммит на новой ветке `feat/...` от main, конвеншен-коммиты, PR в main, мерж через gh
     (squash, по аналогии с PR #1–#9). После мержа — sync клона маркетплейса.

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции. Не-100%-стандартная функция должна быть определена в проекте:
  grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl.
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП (без префикса) — НЕ править.
- Буква ё в именах метаданных ЗАПРЕЩЕНА.
- Новые UUID метаданных — только случайные (uuidgen | tr 'A-Z' 'a-z'), включая
  typeId/valueTypeId в producedTypes; проверка grep -rn "<uuid>" Project/Toir/src.
- 🔴 Правка метаданных XML вслепую (без EDT/MCP) — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА (главный артефакт сессии):
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода>
  Предложение: <что добавить/исправить в плагине>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) Выполни ШАГ 0 — подтверди версию кэша / примени УРОК ДОСТАВКИ до 0.5.6.
2) Предложи пользователю выбор кейса (B приоритет / E) через AskUserQuestion.
3) По выбранному кейсу — двойная маршрутизация → разработка → самопроверка → отчёт →
   фикс в плагине → верификация.
4) Повтори для следующего кейса (цель 1–2 за сессию).
5) Финал: приоритезированный список отклонений + РЕАЛЬНО внесённые фиксы + верификация +
   бамп версии + коммит/PR + обновление этого next-session-prompt.md.

ОЖИДАЕМЫЙ ИТОГ СЕССИИ: плагин 0.5.6 → 0.6+ (если B/СКД даст substantially новый
функционал — minor-бамп; иначе серия patch); каждый кейс оставляет след и в коде toir2,
и в улучшении плагина.
