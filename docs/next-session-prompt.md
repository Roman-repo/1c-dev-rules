# Промпт для переезда в новую сессию — ЗАКРЫТИЕ ПРИЁМКИ TASK-001 (0.16.1)

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, миссия, процедура, стиль, формат отчёта.

---

Контекст: плагин 1c-dev-rules **0.16.1** (main = cac3318, все PR смёржены: #1–#40,
в удалённом репо только main — ветки чистим после мержа), **29 скилов** =
23 разработки + 6 конвейера разработки (Планирование → Проектирование →
Разработка → Внутренняя приёмка → Внешняя приёмка → Релиз). Конвейер прошёл
первый полевой прогон (сессия 9, TASK-001) **до внешней приёмки**: она ОТЛОЖЕНА
(нет живой базы), релиз — черновик.

Рабочий каталог для прикладных задач: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР,
EDT/БСП, платформа 8.3.27). Исходники: Project/Toir/src/.
ПЛАГИН: ~/Projects/1c-dev-rules (github.com/Roman-repo/1c-dev-rules).
Двухплатформенный: ZCode + Kimi Work.

🆕 ИНСТРУМЕНТ (с 0.12.0): `python3 scripts/delivery_tools.py <status|check>
<каталог задачи>` — состояние и DoD-гейты выводятся из артефактов 01–06, без
ручной реконструкции. Для TASK-001 (из корня плагина):
`python3 scripts/delivery_tools.py status ~/Projects/toir2/docs/delivery/TASK-001`

ИСТОРИЯ (кратко; НЕ повторяй):
• Сессии 1–8: прикладные классы + скрипт X1–X15/F10 + слой конвейера (0.11.0),
  терминология «конвейер разработки» (0.11.1). См. CHANGELOG.md.
• Сессия 9, PR #30–#32 (0.11.2–0.11.4): полевой прогон конвейера на TASK-001 —
  компактная печатная форма «Заявка на ремонт (согласование)» для
  торо_ЗаявкаНаРемонт; 7 находок закрыты. Отчёт:
  docs/field-reports/2026-08-15-conveyor-run-task001.md.
• Сессия 10, PR #34–#40 (0.12.0–0.16.1): ревью конвейера → 8 улучшений, все
  реализованы: `delivery_tools.py` status/check (#34); коллизия триггеров
  маршрутизаторов — dispatch-gate только этап «Разработка» (#35); алфавит
  статусов 04 + колонки «Статус 05»/«Статус 06», ✅с (#36); базлайн
  сравнительных критериев + дата пересмотра «Отложено» (#37); ретроспектива-
  петля оценки в 01 (#38); дважды ❌ → обязательный unit/BDD (#39); README —
  этап «Разработка» в таблицу конвейера (#40). Живой базы в сессии не было —
  новые правила ждут полевой проверки.

СОСТОЯНИЕ TASK-001 (каталог ~/Projects/toir2/docs/delivery/TASK-001/):
• Код и макет написаны вслепую (XML без EDT): ManagerModule.bsl
  (ПечатьСогласования + команда «Согласование»), Templates/ПФ_MXL_Согласование,
  mdo. Существующая смета не тронута. Артефакты 01–06 + _releases/2.0.27.0-draft/.
• 05 — статический прогон (✅ статич.), интерактивные шаги ⏳ — на внешней
  приёмке. 06 — «отложено» БЕЗ даты пересмотра (WARN от delivery_tools —
  закрыть при возобновлении).
• Матрица 04 — в старом одно-колоночном формате (до 0.13.0); скрипт читает оба
  формата, переводить не обязательно (при .r1 удобнее двухколоночный).

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ):
1. main = cac3318 или новее; манифесты 0.16.1; CHANGELOG [0.12.0..0.16.1].
2. 🔴 ДВА КАНАЛА ДОСТАВКИ:
   • ZCode: клон маркетплейса ~/.zcode/cli/plugins/marketplaces/1c-dev-rules УЖЕ
     на cac3318. Осталось: ПЕРЕЗАПУСК клиента (+ reinstall плагина, если кэш
     скилов в system-reminder < 0.16.1).
   • Kimi Work: копия в ~/Documents/kimi/workspace/1c-dev-rules ОСТАЛАСЬ на
     0.10.0 (TCC: Operation not permitted, сессии 7–10). Дать терминалу Full
     Disk Access либо вручную: rsync -a --delete --exclude=.git
     ~/Projects/1c-dev-rules/ ~/Documents/kimi/workspace/1c-dev-rules/,
     затем PersonalPluginRegister + обновление в UI.
3. Прогон delivery_tools.py status/check на TASK-001 — сверить с ожиданием:
   этап 5 «Внешняя приёмка — Отложено», ⏳-критерии 1–7, WARN про дату пересмотра.

МИССИЯ СЕССИИ — ЗАКРЫТЬ КОНВЕЙЕР TASK-001 (живой прогон):
1) Пользователь импортирует toir2 в EDT (правки XML вслепую — обязательная
   проверка структуры макета: индексы форматов/шрифтов, области) и запускает
   базу.
2) Внешняя приёмка по матрице 04 (критерии 1–7): меню Печать → вторая команда;
   поля сверкой; итоги vs смета; пустая заявка; нет цен. Результат → новый
   протокол 06-acceptance-protocol.r1.md со ссылкой на «отложенный» +
   🔴 дата пересмотра (0.14.0); статусы приёмщика — в «Статус 06» по алфавиту
   0.13.0 (✅/❌/⏳/n/a).
3) Возвраты по классам A/B/D → 06a-rework-list → правка → повторный прогон
   затронутых шагов. 🔴 Критерий ❌ дважды — следующая итерация закрывает его
   unit/BDD-тестом, не ручной проверкой (0.16.0). Класс C — новая задача.
   При правке артефактов — формат 0.11.3/0.11.4 (наименование-действие;
   сценарий: счастливый путь/отклонения/описание, шаг = один глагол, [Откл]).
4) После «принято»: повысить черновик _releases/2.0.27.0-draft/ до боевого;
   заполнить ретроспективу карточки 01 (оценка/факт, возвраты, вывод —
   DoD «6 → закрытие», 0.15.0); контрольный прогон delivery_tools.py check —
   без ERR/WARN.
5) Отклонения плагина, всплывшие на живом прогоне — отчёт (формат ниже) →
   фикс → PR → bump-version.sh patch. Правки плагина: commit+push; ветку
   после мержа удалять (в удалённом репо только main).

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — правки объектов ТОИР и АРТЕФАКТЫ ЗАДАЧ — БЕЗ
коммитов): ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. Правки
сессий 1–10: ФормаМониторинга (ДС, колонки 53–58), менеджер/макет/mdo
ЗаявкиНаРемонт, docs/delivery/.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.16.1. Маршрутизаторы: задача → 1c-delivery-gate; код внутри
   этапа «Разработка» → 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции: не-100%-стандартная должна быть определена в проекте
  (grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl).
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП — НЕ править. Буква ё в именах
  метаданных ЗАПРЕЩЕНА. Новые UUID — только случайные (uuidgen).
- 🔴 Правка метаданных XML вслепую — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА:
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода/артефакта>
  Предложение: <что добавить/исправить>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) ШАГ 0 — верификация доставки 0.16.1 (перезапуск клиента!) + delivery_tools
   status/check на TASK-001.
2) Спроси пользователя: импортирован ли toir2 в EDT, готова ли живая база.
3) Если да — веди внешнюю приёмку по миссии выше; если нет — предложи помочь
   с импортом/запуском (диагностика EDT вне досягаемости — тогда зафиксируй
   паузу и спроси, чем занять сессию: черновая заметка отчёта 2026-08-15 —
   code-review «ВТ удаляются после использования» (заметка о шаблоне 05 закрыта
   в 0.13.0); либо второй полевой прогон конвейера на независимой M-задаче —
   его можно начинать и не дожидаясь TASK-001).

ОЖИДАЕМЫЙ ИТОГ: 06-acceptance-protocol.r1 = «принято» (или возврат с классами и
цикл правки); релиз повышен из черновика; ретроспектива 01 заполнена;
delivery_tools check без ERR; отклонения живого прогона — отчёт+PR.
После закрытия TASK-001 — обновить этот промпт (docs PR): следующая миссия —
второй полевой прогон конвейера на независимой задаче (M-класс) с опорой на
delivery_tools.py (status/check на каждом гейте).
