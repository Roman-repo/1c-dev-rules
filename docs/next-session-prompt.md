# Промпт для переезда в новую сессию — АНАЛИЗАТОР 0.30.0: ОСТАТОК ЗАМЕЧАНИЙ ДИАГНОСТИКИ (12 блокирующих без карточек, счётчик покрытия, metadata-слой); ПРИ НАЛИЧИИ ЖИВОЙ БАЗЫ — КОД РЕВЬЮ (этап 6) + ВНЕШНЯЯ ПРИЁМКА TASK-001/TASK-002, СОГЛАСОВАНИЕ TASK-003

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, миссия, процедура, стиль, формат отчёта.

---

Контекст: плагин 1c-dev-rules **0.30.0** (вехи: PR #67/#68 — 0.25.1,
bsl_ls_analyze.py — средний слой этапа 6 с md-отчётами ревью и петлёй
исправлений; PR #70 — README: возврат 6→4 с листом замечаний; PR #71 —
0.26.0, харднинг анализатора — 4 слабости: контроль дрейфа диагностик
(check_bsl_ls_drift.py + квартальная джоба), база fixes 32→56, стоимость
петли (--slim-config, --cache-dir — повторный прогон 0,3 с), живой CI BSL LS
(Temurin JDK 21, еженедельно); PR #72/#73 — 0.27.0, README: настройка BSL LS,
назначение/использования, CONTRIBUTING + шаг 4 в 1c-project-bootstrap;
PR #76 — 0.27.1, фикс slim-config (отключения кладутся в
diagnostics.parameters — плоская карта игнорируется BSL LS 1.0.7, живой
тест 45→13 замечаний); PR #77 — 0.27.2, **правило двух отчётов**
(1c-code-review: отчёт ревью → проект системы, отклонения плагина →
docs/field-reports/ плагина) + оценочный прогон этапа 6 на живом модуле
торо_ВыявленныеДефекты; PR #79 — 0.28.0, сканер №733 ловит ВТ без периода;
PR #80 — 0.29.0, 5 улучшений анализатора: ALIAS→110 пар, склейка
многострочных запросов, дедуп находок слоёв, режим покрытия, fixes до 141;
PR #81 — 0.30.0, **петля качества**: автослияние слоя 1 в отчёт обёртки
(дефолт; --no-merge-scan выключает, --merge-scan — готовый json), контур
подавлений «не баг» из 05a (suppress.json + scripts/review_suppress.py +
--suppress у обоих слоёв; причина обязательна, подавления видны счётчиком
и секцией «Подавленные»), фиксы ложняков (CommentedOutCodeLine на
описательных комментариях; MagicNumber — проектный конфиг
.checkbsl_scan.json, ключ allow-numbers); PR #83 — тесты механик 0.30.0
(+22, сьют 201); ранее
PR #65 — манифест Kimi + автосверка std/ с upstream; ветки после мержа
чистим), **30 скилов** =
22 разработки + 8 конвейера
(**8-этапный** цикл, 0.23.0: Планирование → Проектирование → Согласование →
Разработка → Внутренняя приёмка → **Код ревью** → Внешняя приёмка → Релиз;
уровень выше — эпики; DoR постановки — 0.20.0; этапные «листы решений»:
04a согласование, 05a код ревью). Конвейер прошёл **три полевых прогона**:
TASK-001 (сессия 9), TASK-002 (сессия 11, из трекера) — оба доведены до
внутренней приёмки и ждут живой базы; TASK-003 (GCSRU-1901) — этапы 1–2,
ждёт согласования пакета.

Рабочий каталог для прикладных задач: ~/Projects/toir2 (1С:Предприятие 8 —
ТОИР, EDT/БСП, платформа 8.3.27). Исходники: Project/Toir/src/.
ПЛАГИН: ~/Projects/1c-dev-rules (github.com/Roman-repo/1c-dev-rules).
Двухплатформенный: ZCode + Kimi Work (судьба Kimi-канала — см. ШАГ 0).

ИНСТРУМЕНТ: `python3 scripts/delivery_tools.py <status|check|roadmap>
<каталог>` — состояние и DoD-гейты из артефактов 01–06 (+04a/05a);
8-этапная модель (0.23.0: 05a→6, протокол→7, «принято»→8); roadmap — сводка
всех задач/эпиков каталога доставки.
КОД РЕВЬЮ, ДЕТЕРМИНИРОВАННЫЕ СЛОИ (0.30.0): `python3 scripts/bsl_ls_analyze.py
<файлы/каталог> [--diff REF] [--src-root DIR] [--report FILE] [--slim-config]
[--cache-dir DIR] [--suppress FILE] [--no-merge-scan] [--format md|json]>` —
ядро полноты: bsl-language-server CLI, 129/322 правил каталога — 40%
(слои 1+2 — 137, 42,5%; измерено на 0.29.0); слой 1 (checkbsl_scan.py)
запускается обёрткой АВТОМАТИЧЕСКИ на тех же входах и сливается с дедупом
по ключ/файл/строка — отдельный прогон сканера не нужен (только машины без
Java: `python3 scripts/checkbsl_scan.py <файлы/каталог> [--diff REF]
[--suppress FILE] [--format md|json]>`, exit 1 при 🔴). Подавления «не баг»
из 05a: `python3 scripts/review_suppress.py add <suppress.json> <Ключ>
<файл> [--line N] --reason "…"` → оба слоя с `--suppress suppress.json`
молчат по этой находке (счётчиком честно показывают подавленное).
(0.26.0: --slim-config — диагностики вне каталога∪ALIAS отключены, с 0.27.1
кладутся в diagnostics.parameters — плоская карта игнорируется BSL LS
1.0.7; --cache-dir
— кэш bsl-json.json по хэшу входов, повтор без правок ≈0,3 с; Java 21+ + jar —
на этой машине установлены, JDK 26 подходит; exit 3 = слой недоступен).
Дрейф моста имён контролирует scripts/check_bsl_ls_drift.py (квартально,
джоба check-bsl-ls-drift + тест-замок покрытия ≥109/186). 🔴 ПОРЯДОК
ПОВЕДЕНИЯ (петля самоочистки, 1c-code-review): прогон обоих слоёв на диффе
задачи с `--report docs/delivery/<ЗАДАЧА>/code-review/bsl-ls-r<N>.md` (отчёт:
код + что не так + как правильно) → есть 🔴 → возврат на этап «Разработка»,
правка по отчёту, повторный прогон (r<N+1>) — пока отчёт чист по 🔴; 🟡
правятся в петле или обосновываются в 05a; раунды хранятся в каталоге задачи.

ТРЕКЕР: Project Control Tower на этой машине — http://localhost (SPA; данные
через API `/api/v1/*`, Bearer-токен: POST /api/v1/auth/login c
username/password). TASK-002 = задача 1030 (GCSRU-1763); TASK-003 = GCSRU-1901
(номер задачи в трекере уточнить); источник TASK-001 уточнить у пользователя.
Доступы локальные, у пользователя — в репо НЕ писать.

ИСТОРИЯ (кратко; НЕ повторяй):
• Сессии 1–8: прикладные классы + скрипт X1–X15/F10 + слой конвейера (0.11.0).
• Сессия 9, PR #30–#32: прогон №1 — TASK-001, печатная форма «Заявка на ремонт
  (согласование)». Отчёт: docs/field-reports/2026-08-15-conveyor-run-task001.md.
• Сессия 10, PR #34–#42: ревью конвейера → 8 улучшений.
• Сессия 11 (2026-08-22), PR #43–#44 + 0.17.0: эпики/ADR/лимит; интеграция с
  трекером; прогон №2 — TASK-002 (GCSRU-1763): этапы 1–4, отклонение №1
  (легенда 04 → ложные ERR) — фикс+тест. Отчёт:
  docs/field-reports/2026-08-22-conveyor-run-task002.md.
• Сессия 12 (2026-08-23), PR #46→#53 (0.18.0→0.20.0): согласование пакета →
  полновесный этап 3 с листом 04a; режимы /auto //manual; DoR-гейт,
  обогащённая 01, НФТ/макеты в 03.
• 2026-08-23, PR #55→#59 (0.21.0→0.22.3): **прогон №3 — TASK-003**
  (GCSRU-1901, «Повторный отказ» ЕВО-бланк): этапы 1–2 за сессию; практика
  прогона поднята в плагин (оценка в чел.-ч таблицей, секция «Решения
  инициатора», дедуп «Оценки затрат» в шапке 01); README-фиксы: бейдж и H1
  версии синхронизируются bump-version.sh.
• 2026-08-29, PR #60 (0.23.0): **этап 6 «Код ревью»** — роль Ревьюера,
  артефакт 05a-code-review.md («Одобрено»/«Доработать», возврат на этап 4),
  каталог правил checkbsl в 1c-code-review (references/checkbsl/; генератор
  scripts/harvest_checkbsl.py; 322 правила: код 256 / запросы 34 / метаданные
  32, аудит-Б 24; важность из BSL LS НЕ подтягивается — join по названию
  ненадёжен, приоритет — чек-лист 🔴🟡🟢 и стандарт); delivery_tools 8 этапов,
  гейт 6→7; режим _conveyor-mode.md действует на оба живых гейта (3 и 6);
  строка «code-review ✅» в шапке 05 — самопрогон разработки, НЕ решение
  Ревьюера; CI: check_version_sync.py + unittest discover (59 тестов).
• 2026-08-29, PR #62 (0.23.1): **description всех 30 скилов ≤250 симв** —
  слова-триггеры перенесены в видимую зону индекса (окно ZCode ~250 симв
  эмпирично; у 11 скилов блок «Триггеры:» лежал целиком за границей обрезки,
  включая оба маршрутизатора и 1c-code-review). Формат файлов, when_to_use и
  тела не менялись; валидатор 30 WARN → 0. 🔴 Конвенция на будущее: описание
  скила ≤250 симв, триггеры внутри видимой зоны; when_to_use в индекс скилов
  НЕ попадает; DESC_SOFT_LIMIT поднимать только вместе с фактическим
  расширением окна клиентом (ARCHITECTURE.md «Триггеры скилов»).
• 2026-08-30, сессия 13: **пробный прогон этапа 6** на типовом модуле
  торо_ЗакрытиеЗаявокИРемонтов (3 🔴: запрос в цикле, голый СрезПоследних,
  чтение ТЧ через точку в цикле; рубрикация code/query подтверждена).
  Замер покрытия требования «все правила чек-листа bsl применяются»:
  regex-слой ~7–10% детерминированно, ИИ-проход ~60–70% номинально,
  BSL LS CLI ~85–95% детерминированно → решение о трёх слоях:
  сканер → BSL LS (ядро полноты) → ИИ-интерпретация.
• 2026-08-30, PR #64 (0.24.0): **детерминированный regex-слой checkbsl** —
  scripts/checkbsl_scan.py (~22 ключа каталога, --diff, md/json, exit 1 при
  🔴), режим «пакет-первым» в 1c-code-review (сканер → каталог → чек-лист),
  58 тестов (suite 117). Полевой контроль: прогон на том же модуле воспроизвёл
  детерминированное подмножество ручного ревью без ложных срабатываний.
• 2026-08-30, PR #65 (0.25.0): **манифест Kimi по реальной схеме** (interface,
  skills-контейнер, commands; плагин зарегистрирован в маркете Kimi) +
  **автосверка std/ с upstream v8std** (scripts/check_upstream_std.py,
  workflow upstream-sync.yml поквартально, найден и добавлен №799; живой
  прогон 318=318). Ветка пересобрана поверх main, версия 0.25.0.
• 2026-08-30, сессия 14, PR #67–#74 (0.25.1 → 0.27.0, всё в main): **ядро
  полноты checkbsl — bsl_ls_analyze.py** (средний слой этапа 6): обёртка
  bsl-language-server CLI (analyze → bsl-json.json), алиасы ключей BSL LS на
  каталог (~65 пар — системы имён разные), серьёзность чек-лист → важность
  BSL LS, дифф по файлам и добавленным строкам, --src-root для контекста
  конфигурации (без Configuration.mdo запросы не разбираются), graceful
  degradation (нет Java/jar → exit 3, слой 1 — сканер); harvest_bsl_ls.py +
  bsl_ls_diagnostics.json (186 диагностик); секция «Детерминированные слои» в
  1c-code-review. Вторым коммитом: **--report** — md-отчёт ревью в каталоге
  задачи (docs/delivery/<ЗАДАЧА>/code-review/bsl-ls-r<N>.md: код с маркером
  строки + «что не так» + «как правильно» + вердикт) на базе знаний
  bsl_ls_fixes.json и **петля самоочистки** в скиле (🔴 → возврат на этап
  «Разработка» → правка → повторный прогон r<N+1> до чистого; 🟡 правятся или
  обосновываются в 05a). Живой прогон на полной конфигурации ТОИР
  (2627 модулей): модуль формы торо_ЗакрытиеЗаявокИРемонтов — 3 🔴
  VirtualTablesWithoutInnerFilter №733 (детерминированное подтверждение
  ручного ревью), объектный модуль — 0. Далее параллельным треком (PR #71,
  0.26.0) — харднинг: check_bsl_ls_drift.py (дрейф моста имён, квартально) +
  тест-замок покрытия ≥109/186; fixes-база 32 → 56 записей; --slim-config и
  --cache-dir (повторный прогон петли ≈0,3 с); CI-джоба живого BSL LS
  (bsl-ls-live.yml, Temurin JDK 21 — на 17 exec.jar падает в JarLauncher;
  скачивание jar — по tag через API, latest-ассет даёт 404). PR #70 — README:
  возврат 6→4 с листом замечаний код ревью в таблице артефактов; аудит
  возвратов конвейера (3→2/1 по 04a, 5→4 по красным строкам 05, 6→4 по
  05a+петля, 7→возврат по 06+06a A/B/C/D) — дока сходится с реализацией.
  PR #72/#73 (0.27.0) — README: «Настройка анализатора кода (BSL LS)»,
  назначение/сценарии использования, установка BSL LS в CONTRIBUTING и
  1c-project-bootstrap (шаг 4). Сьют 156 тестов. Клон маркетплейса ZCode
  синхронизирован (на момент сессии 14 — cd52787). НА МАШИНЕ УСТАНОВЛЕНО:
  openjdk 26 (brew,
  keg-only — /opt/homebrew/opt/openjdk/bin/java), jar v1.0.7 —
  ~/.local/share/1c-dev-rules/bsl-language-server.jar.
• 2026-08-30, сессия 15, PR #76/#77 (0.27.1 → 0.27.2, всё в main): **фикс
  slim-config + оценочный прогон этапа 6 на живом модуле**. PR #76: slim-config
  РЕАЛЬНО отключает диагностики — плоская карта {"diagnostics": {…: false}}
  молча игнорируется BSL LS 1.0.7, персональные настройки живут в
  diagnostics.parameters (живой тест на модуле документа ТОИР: 45→13
  замечаний; 2627 модулей за 67 с, повтор по кэшу 2,7 с). Оценка этапа на
  ObjectModule.bsl торо_ВыявленныеДефекты (3 слоя + чек-лист; вердикт
  «Доработать», 12 замечаний 🔴3/🟡6/🟢3): трёхслойность НЕ избыточна — 🔴
  №783 (запрос и записи статусов внутри транзакции проведения) и 🟡 №648
  (точечные чтения одной ссылки ×N) не выражаются AST-диагностиками BSL LS,
  ловит только ручной слой чек-листа. PR #77: **правило двух отчётов** —
  отчёт ревью (05a + «код → почему → как исправить» + отчёты слоёв r<N>)
  в проект системы (задача: docs/delivery/<ЗАДАЧА>/code-review/, вне
  задачи: docs/code-review/), отчёт об отклонениях плагина — в
  docs/field-reports/ плагина без дублирования текста ревью. Отчёты
  прогона: отклонения — docs/field-reports/2026-08-30-code-review-eval-
  toro-VyavlennyeDefekty.md; ревью и слои r1 — в toir2/docs/code-review/
  (включая LIVE-тест торо_ВыбытиеОбъектаРемонта, перенесён туда же);
  клон маркетплейса синхронизирован с f0f0d75.
• 2026-08-30…31, сессия 16, PR #79–#83 (0.28.0 → 0.30.0, всё в main): **разгон
  анализатора по диагностике качества**. PR #79 (0.28.0): сканер №733 ловит ВТ
  с пустым первым параметром (СрезПоследних(, Отбор)). PR #80 (0.29.0):
  ALIAS +24 пары (→110), склейка многострочных текстов запросов в сканере,
  дедуп слоёв (--merge-scan), режим замера покрытия, fixes-база → 141 запись
  (каждая детерминированная находка гарантированно с «что не так» + «как
  правильно»). Диагностика пользователя: покрытие 36% → 42,5% (137/322);
  устранены 3 системных дефекта — молчаливая потеря полноты в slim, дубли
  находок, слепота к многострочным запросам. PR #81 (0.30.0): контур
  подавлений + автослияние слоя 1 + фиксы ложняков (детали — в «Контекст»);
  вмержен параллельно с моей реализацией (два потока работы над одной
  веткой — координировать через этот промпт). PR #83: тесты механик 0.30.0
  (+22: review_suppress, load_suppress/suppression_for, CLI --suppress,
  load_project_allow_numbers, run_layer1/filter_diff_lines/apply_suppressions,
  секция «Подавленные»), сьют 201, клон 29bd2f3. Живой конвейерный тест
  пользователя: docs/field-reports/…review-pipeline-0.29.0-toro-
  ZayavkaNaRemont.md. Маршрут остатка замечаний согласован — см. миссию
  п.10.

СОСТОЯНИЕ TASK-003 (~/Projects/toir2/docs/delivery/TASK-003/):
• Прогон №3 по полному циклу 0.20.0 (DoR, обогащённая 01). Этап 2 завершён:
  01–04 готовы, 11 критериев в матрице. Следующий шаг — этап 3 «Согласование»
  (в toir2 файла _conveyor-mode.md нет → manual): предъявить сводку пакета
  02/03/04 Оркестратору (пользователю) → лист 04a; 🔴 код не начинается до
  «Согласовано». После разработки это будет первый прогон этапа 6 «Код
  ревью» (05a + каталог checkbsl).

СОСТОЯНИЕ TASK-002 (~/Projects/toir2/docs/delivery/TASK-002/):
• 01–05 готовы, этап 5 завершён. С 0.23.0 следующий шаг — **этап 6 «Код
  ревью»**: петля самоочистки (сканер + bsl_ls_analyze.py --report в
  code-review/ — до чистого по 🔴), затем чек-лист 1c-code-review + каталог
  checkbsl → лист 05a; после «Одобрено» — внешняя приёмка на живой базе по
  матрице 04 (критерии 1–9; интерактивная проверка — инструкции в колонке
  «Проверка»).
• Код написан вслепую, БЕЗ EDT (3 файла): ManagerModule.bsl
  торо_ИдентификаторыТехКарт (ДопустимаУстановкаСтатусаДействует), ФормаЭлемента
  (проверки только на СМЕНУ статуса; обёртка НаСервереБезКонтекста; фильтр
  ФильтрФайловТехКарт в ДобавитьФайлОперации), торо_ОбщегоНазначенияКлиентСервер
  (ФильтрФайловТехКарт + ТекстОшибкиУтвержденияТехКартыБезФайла). 🔴 первый
  импорт EDT = обязательная проверка компиляции.
• ADR-001/ADR-002 — ~/Projects/toir2/docs/architecture/adr/: проверка в модуле
  менеджера, НЕ в объектном (ломает миграции ИБ); группа «Ключевой
  пользователь» через РС СоставыГруппПользователей, НЕ найдена → запрет для
  всех (fail-closed).
• Решения инициатора (зафиксированы в 01): наличие ЛЮБОГО файла снимает запрет;
  PDF только в фильтре техкарт (ТиповыеОР/ОбъектыРемонта не тронуты).

СОСТОЯНИЕ TASK-001 (~/Projects/toir2/docs/delivery/TASK-001/):
• Код и макет написаны вслепую (менеджер ЗаявкиНаРемонт + ПФ_MXL_Согласование);
  05 — статический прогон. С 0.23.0 задача на этапе 7 «Внешняя приёмка» —
  решение «Отложено» (2026-08-15; нумерация этапов ≥6 сместилась +1). Перед
  возобновлением приёмки — этап 6 «Код ревью» (петля самоочистки + лист 05a).
• 06 — «отложено» БЕЗ даты пересмотра (WARN от delivery_tools — закрыть при
  возобновлении). Матрица 04 — старый одно-колоночный формат (скрипт читает
  оба). Релиз: _releases/2.0.27.0-draft/.

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ):
1. main = мерж PR #83 (0.30.0) или новее (git log: «Merge pull request #83»);
   манифесты 0.30.0; CHANGELOG [0.22.3..0.30.0]; сьют 201 зелёный.
2. 🔴 ДВА КАНАЛА ДОСТАВКИ:
   • ZCode: клон маркетплейса ~/.zcode/cli/plugins/marketplaces/1c-dev-rules
     синхронизирован с main в конце сессии 16 (29bd2f3) — проверить
     git -C <клон> log -1; ПЕРЕЗАПУСК клиента обязателен (+ reinstall, если
     после перезапуска кэш скилов < 0.30.0). До перезапуска этапные
     скилы читать файлами
     из ~/Projects/1c-dev-rules/skills/ — репо = источник истины.
   • Kimi Work: канал ЖИВ, судьба решена (PR #65: манифест в схеме Kimi —
     interface/skills-контейнер/commands; плагин зарегистрирован в маркете,
     kimi-work://plugin?id=1c-dev-rules). 🔴 копия
     ~/Documents/kimi/workspace/1c-dev-rules НЕЧИТАЕМА из песочницы ZCode
     (TCC: «Operation not permitted»; вывод прошлой сессии «каталога нет»
     неверен — каталог есть): обновлять до 0.30.0 rsync-ом из терминала
     ПОЛЬЗОВАТЕЛЯ (rsync -a --delete --exclude=.git ~/Projects/1c-dev-rules/
     ~/Documents/kimi/workspace/1c-dev-rules/) или выдать ZCode доступ к
     ~/Documents. В Kimi нет команд — режим конвейера правкой
     _conveyor-mode.md.
3. delivery_tools status/check на TASK-001/002/003 — сверить (8-этапная модель
   0.23.0): TASK-002 — этап 5 завершён, следующий шаг «код ревью»; TASK-003 —
   этап 2, следующий шаг «согласование»; TASK-001 — этап 7, «Отложено». WARN
   задач, начатых до введения этапов: нет 04a (до 0.19.0) и нет 05a (до
   0.23.0) — легально, листы можно создать задним числом; TASK-001 — дата
   пересмотра.

МИССИЯ СЕССИИ — ПРИОРИТЕТ: ОСТАТОК ЗАМЕЧАНИЙ ДИАГНОСТИКИ АНАЛИЗАТОРА (п.10;
по указанию пользователя сессия начинается с него) + ПРИ ГОТОВНОЙ ЖИВОЙ
БАЗЕ — КОД РЕВЬЮ И ЗАКРЫТЬ ОБЕ ЗАДАЧИ (п.1–7):
1) Пользователь импортирует toir2 в EDT (🔴 правки XML вслепую ОБЕИХ задач —
   обязательная структурная проверка при импорте: макет ПФ TASK-001 — индексы
   форматов/шрифтов, области; менеджер/форма/ОМ техкарт TASK-002) и запускает
   базу.
2) Этап 6 «Код ревью» ОБЕИХ задач (первое применение на живом прогоне).
   Сначала — 🔴 петля самоочистки (0.25.1+, порядок Исполнителя): оба слоя на
   диффе задачи с --report docs/delivery/<ЗАДАЧА>/code-review/bsl-ls-r1.md
   (сканер — checkbsl-r1.md); есть 🔴 → возврат на этап 4, правка по отчёту
   («как правильно» — отправная точка), повторный прогон r<N+1> — пока отчёт
   чист по 🔴 (🟡 правятся или обосновываются в 05a). Затем Ревьюер
   (пользователь или ИИ по его поручению) проходит чек-лист 1c-code-review по
   секциям типа изменений + разделы каталога checkbsl (модули/форма TASK-002 —
   code.md; запросы TASK-001 — query.md; правки метаданных — metadata.md) +
   high-risk §8 целиком; findings — по шаблону комментария MR скила; решение в
   05a-code-review.md (шаблон — references скила). «Доработать» → возврат на
   этап 4, правка, повтор затронутых проверок 05 и повторное ревью тем же
   файлом.
3) Внешняя приёмка TASK-002 по матрице 04 (критерии 1–9): запрет и сообщение;
   ключевой пользователь; PDF-загрузка; серверная защита (обход клиента);
   перезапись действующей карты; группа не найдена → запрет для всех; Типовые
   ОР без PDF. Для критерия 3/8 нужен тестовый пользователь в группе «Ключевой
   пользователь» (создать в базе). Результат → 06-acceptance-protocol.md,
   статусы приёмщика в «Статус 06» (✅/❌/⏳/n/a).
4) Внешняя приёмка TASK-001 по её матрице (критерии 1–7): меню Печать → вторая
   команда; поля сверкой; итоги vs смета; пустая заявка; нет цен. Результат →
   новый протокол 06-acceptance-protocol.r1.md со ссылкой на «отложенный» +
   🔴 дата пересмотра.
5) Возвраты по классам A/B/D → 06a-rework-list → правка → повторный прогон
   затронутых шагов. 🔴 Критерий ❌ дважды — следующая итерация закрывает его
   unit/BDD-тестом. Замечания ревью (05a) идут без классов A–D: возврат на
   этап 4. При правке артефактов — формат 0.11.3/0.11.4; новые/переписываемые
   секции 01/03 — по шаблонам 0.20.0+ (DoR, бизнес-ценность метриками, чел.-ч,
   НФТ, макет формы).
6) После «принято»: повысить _releases/2.0.27.0-draft/ до боевого, состав —
   обе задачи; заполнить ретроспективы карточки 01 ОБЕИХ задач; контрольный
   delivery_tools check — без ERR/WARN (дата пересмотра TASK-001 закроет WARN).
7) Обновить статусы в трекере (http://localhost/tasks/1030 = TASK-002;
   TASK-003 = GCSRU-1901, номер уточнить; TASK-001 — источник уточнить) —
   через UI или API пользователя.
8) Отклонения плагина, всплывшие на живом прогоне — отчёт (формат ниже) →
   фикс → PR → bump-version.sh patch. Известные наблюдения (кандидаты на
   маленькие PR): (а) этап 4 «Разработка» невидим для статус-модели
   delivery_tools — файлового маркера нет, status перепрыгивает 3 (по 04a) → 5
   (по 05); актуально и в 8-этапной модели (field-report 2026-08-22);
   (б) каталог checkbsl — рубрикация code/query подтверждена пробным ревью
   2026-08-30 (объектный модуль), осталось проверить на диффе живой задачи;
   (в) пробелы каталога из пробного ревью: нет ключа на чтение через точку к
   данным других объектов в цикле (№436/№648-паттерн), нет ключа на построчную
   запись наборов/Движений в цикле (№792), нет пункта про ТабличнаяЧасть.Найти()
   без колонки (ложные совпадения в чужих колонках); (г) из оценочного
   прогона 2026-08-30 (полный список — field-report code-review-eval):
   ✅ ложняк сканера CommentedOutCodeLine закрыт 0.30.0; осталось —
   расширение (в): точечные чтения ОДНОЙ ссылки вне цикла
   (Основание.Поле ×4 ветки ≈ 18 неявных запросов — карточка + пункт
   §5); пример fixes для MethodCognitiveComplexity нерелевантен ветвлению
   ТипЗнч (кандидат в bsl_ls_fixes.json); НСтр-качество переводов (битые
   es-строки) не покрыто ничем — кандидат в 1c-localization.
9) ✅ ВЫПОЛНЕНО (сессия 14, PR #67–#74 / 0.25.1→0.27.0, в main): ядро
   полноты пакета checkbsl — bsl-language-server CLI + отчёты ревью + петля
   самоочистки, захарднено (дрейф-контроль, fixes 56, slim-config+кэш, живой
   CI). JDK (openjdk 26, brew) и jar v1.0.7
   (~/.local/share/1c-dev-rules/bsl-language-server.jar) установлены;
   scripts/bsl_ls_analyze.py с алиасами на каталог, дифф-фильтрацией по
   файлам/строкам, --report (код + что не так + как правильно + вердикт),
   --slim-config/--cache-dir и graceful degradation (exit 3); база
   bsl_ls_fixes.json (56 записей); петля закреплена в 1c-code-review;
   сьют 156. Трёхслойная проверка кода собрана целиком. Продолжение по мере
   живых ревью: пополнять ALIAS (ключ без алиаса — кандидат) и
   bsl_ls_fixes.json (замечание без примера «как правильно» — кандидат);
   тесты и check_bsl_ls_drift.py держат целостность обеих таблиц.
10) 🔴 ОСТАТОК ЗАМЕЧАНИЙ ДИАГНОСТИКИ АНАЛИЗАТОРА (диагностика пользователя
   2026-08-31; маршрут согласован; шаг 1 — автослияние/подавления/фиксы
   ложняков — ✅ ЗАКРЫТ в 0.30.0, PR #81/#83). Приоритет сессии 17 — по
   указанию пользователя продолжаем с этого пункта:
   (а) **12 блокирующих немапленных диагностик BSL LS** (из 53 немапленных;
   примеры: MissingTempStorageDeletion, ServerCallsInFormEvents,
   CommonModuleVariables): сейчас в полном режиме дают находки без №
   стандарта и fixes, в slim — выключены, т.е. слепота по блокирующим.
   Решение (расширение КАТАЛОГА, не моста): локальные карточки — НЕ в
   harvest-каталог references/checkbsl/ (его регенерит harvest_checkbsl.py,
   свои ключи конфликтуют), а отдельной таблицей (LOCAL_CATALOG в скрипте
   или references/checkbsl/local.md), подмешиваемой в load_catalog(); на
   каждую — № стандарта (у части есть: ServerCallsInFormEvents → №487/№636)
   + запись в bsl_ls_fixes.json («что не так» + «как правильно»); исключить
   12 из off-множества slim_config (оставить включёнными — гарантия 0.29
   «каждая находка с почему+как» сохраняется); замок в check_bsl_ls_drift.py
   («12 ключей имеют карточку+fix»). Оценка M, 0.30.x/0.31.
   (б) **Счётчик покрытия в скрипте** (--coverage-report или через drift):
   «42,5% (137/322)» должно быть воспроизводимым числом из кода, а не
   ручным подсчётом; обновляемая строка — в README-статус. Оценка S.
   (в) **Эпик «metadata-слой»** (оценка L — декомпозиция по 1c-epic-planning,
   НЕ одним PR): парсинг .mdo/.rights → 28 metadata-ключей каталога
   детерминированно (права №532/№5, синонимы ru/en, ё в именах, порядок
   метаданных — не видны ни regex, ни AST). MVP: консолидация
   scripts/validate-new-object.sh (уже проверяет UUID/роли/подсистемы новых
   объектов) в python-слой с маппингом на каталог и вливанием в общий
   конвейер (merge/suppress/отчёт); дальше — существующие объекты.
   (г) **Честная фиксация потолка в доках**: 187 ключей (58%: 136 overall,
   28 metadata, 23 query) — на ИИ-проходе, это структурный потолок связки
   regex+AST (нужны данные вне текста и AST одной конфигурации); рост —
   не алиасы, а источники (п. в). Отразить в README/ARCHITECTURE.md.

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — правки объектов ТОИР и АРТЕФАКТЫ ЗАДАЧ — БЕЗ
коммитов): ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. Правки
сессий 1–10: ФормаМониторинга (ДС, колонки 53–58), менеджер/макет/mdo
ЗаявкиНаРемонт, docs/delivery/TASK-001. Правки сессии 11: менеджер + форма
торо_ИдентификаторыТехКарт, торо_ОбщегоНазначенияКлиентСервер,
docs/delivery/TASK-002, docs/architecture/adr/ADR-001/ADR-002. Прогон №3:
docs/delivery/TASK-003 (карточка + пакет 01–04).

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.30.0 (8 этапов: …Внутренняя приёмка → Код ревью → Внешняя
   приёмка…). Маршрутизаторы: задача → 1c-delivery-gate; код внутри этапа
   «Разработка» → 1c-dispatch-gate; инициатива дороже задачи →
   1c-epic-planning. Живые гейты: этап 3 «Согласование» (решение
   Оркестратора в 04a) и этап 6 «Код ревью» (решение Ревьюера в 05a) — режим
   manual-дефолт (в toir2 файла _conveyor-mode.md нет). Гейт 1→2 включает
   DoR (1c-planning → references/dor-checklist.md).
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции: не-100%-стандартная должна быть определена в проекте
  (grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl).
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП — НЕ править. Буква ё в именах
  метаданных ЗАПРЕЩЕНА. Новые UUID — только случайные (uuidgen).
- 🔴 Правка метаданных XML вслепую — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».
- Скилы (правка/создание): description ≤250 симв, слова-триггеры — внутри
  видимой зоны (окно индекса ~250; when_to_use в индексе невидим); цель —
  0 WARN валидатора по длине.
- Новые правила проверки кода — декларативные записи в RULES сканера
  (scripts/checkbsl_scan.py): ключ каталога + severity + паттерн + scope;
  каждое с парой тестов позитив/негатив; свой движок-анализатор НЕ пишем —
  полнота через BSL LS (scripts/bsl_ls_analyze.py); новые примеры «как
  правильно» — записи в scripts/bsl_ls_fixes.json (тест держит ключи).

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА:
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода/артефакта>
  Предложение: <что добавить/исправить>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

Отчёт ревью при этом живёт в ПРОЕКТЕ СИСТЕМЫ под ревью (задача:
docs/delivery/<ЗАДАЧА>/code-review/; вне задачи: docs/code-review/) —
в плагин только отклонения, без дублирования текста ревью (правило двух
отчётов, 1c-code-review, 0.27.2).

С ЧЕГО НАЧАТЬ:
1) ШАГ 0 — верификация доставки 0.30.0 (клон уже синхронизирован; перезапуск
   клиента!) + delivery_tools status/check на трёх задачах.
2) Веди миссию п.10 — остаток замечаний диагностики (приоритет сессии по
   указанию пользователя): сначала (а) карточки 12 блокирующих + fixes +
   включение в slim + замок drift; затем (б) счётчик покрытия; (в) —
   декомпозиция эпика metadata-слоя на задачи (карточки 01) без реализации
   одним куском.
3) Живая база: спроси пользователя, импортирован ли toir2 в EDT. Если да —
   чередование/переключение на живую базу (этап 6 обеих задач + приёмки —
   миссия п.2–7). Если нет — п.10 занимает сессию целиком; запасные треки:
   (а) согласование TASK-003 с Оркестратором; (б) фиксы-наблюдения п.8
   (этап 4 без маркера; пробелы каталога (в)/(г)); (в) сверка проектного
   DoR toir2 с плагинным 0.20.0+.

ОЖИДАЕМЫЙ ИТОГ: миссия п.10 — (а) 12 блокирующих немапленных диагностик
получили локальные карточки + записи fixes + включены в slim + замок в
check_bsl_ls_drift.py; (б) покрытие считается командой скрипта (число
воспроизводимо, README-статус обновляется из него); (в) эпик metadata-слоя
декомпозирован (карточки задач с лимитами), MVP согласован или начат;
(г) README/ARCHITECTURE честно фиксируют потолок 58% и путь роста. Живая
база (если готова): 05a обеих задач = «Одобрено»; 06-протоколы обеих задач =
«принято» (или возвраты с классами и цикл правки); релиз 2.0.27.0 повышен из
черновика (состав: обе задачи); ретроспективы 01 обеих задач заполнены;
delivery_tools check обеих — без ERR; статусы в трекере обновлены; отклонения
живого прогона — отчёт+PR. TASK-003 — согласован и (если сессия позволяет)
развит по циклу. Трёхслойная проверка кода (сканер → BSL LS → ИИ) с 0.30.0 —
с автослиянием и контуром подавлений; на живых ревью TASK-001/002 этап 6
идёт запуском ОДНОЙ команды обёртки (--src-root Project/Toir/src). После
закрытия — обновить этот промпт (docs PR): якоря новой версии + миссия по
накопленным ретроспективам.
