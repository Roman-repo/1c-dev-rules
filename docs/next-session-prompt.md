# Промпт для переезда в новую сессию — после 7-го полевого тестирования (0.10.0)

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, цель, процедура, стиль, формат отчёта.

---

Контекст: плагин 1c-dev-rules после СЕДЬМОГО полевого тестирования — **0.10.0**,
**полевое тестирование ИСЧЕРПАНО** (все крупные классы задач + все известные
пробелы закрыты: кейсы A–G, E', сессии 1–2, P4). Дальше — эксплуатация и новые
классы задач по мере появления.

Рабочий каталог для прикладных задач: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР,
EDT/БСП, платформа 8.3.27, ~2822 BSL-модуля). Исходники: Project/Toir/src/.
ПЛАГИН: ~/Projects/1c-dev-rules (github.com/Roman-repo/1c-dev-rules). Все PR
смёржены: #1–#24 (+ docs-PR промпта). Двухплатформенный: ZCode + Kimi Work.

СЕССИЯ 7 (кейсы E' + P4) — PR #24, 0.9.0→0.10.0:
- **Кейс E' (ДС под нагрузкой)**: доработка ФормыМониторинга торо_ОбъектыРемонта —
  колонка «Категория риска» (гкс_КатегорияРиска, ссылка, БЕЗ соединения) +
  «Критичность дефекта» (реквизит торо_КатегорииРиска через явное ЛЕВОЕ
  СОЕДИНЕНИЕ с обычной таблицей — образец паттерна №732, не точка).
- **X15** (validate): анализ queryText ДС в формах — СОЕДИНЕНИЕ с ВТ/подзапросом
  → FAIL (№655/№732), ВТ без даты → WARN (№733), ОБЪЕДИНИТЬ без ВСЕ → WARN (№434).
  Точки (№732) не проверяются — ручной grep (ложные срабатывания на источники).
- **X14** (validate, B-3 закрыт): &параметр <query> ↔ <parameter> схемы СКД;
  разбор с явным namespace (dcscor:parameter — другой ns, не объявления).
- **№732-лестница** (1c-queries): колонка-ссылка без соединения → реквизит через
  явное ЛЕВОЕ СОЕДИНЕНИЕ → точка запрещена. **№636-граница** (1c-performance):
  дорого только клиент→сервер; сервер→сервер (из ПриСозданияНаСервере) контекст
  не гонит — не обвинять в ревью.
- Ревью вендорской ФормаЭлемента (9 ДС): истинные №655/№732/№733/№434 в
  ПредстоящиеРемонты/ПредстоящиеРегМероприятия/ВыполненныеРемонты/
  ВыполненныеРегМероприятия. Код поставщика — НЕ править (негативный полигон X15).

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ):
1. В system-reminder проверь **23 скила** 1c-* и версию **0.10.0** в путях кэша.
2. Маркеры 0.10.0 в ~/Projects/1c-dev-rules (main, коммит 3b1faf6 или новее):
   • `scripts/validate-new-object.sh` — «X2/X3/X4/X15», «X15», «X14», «B-3/X14»;
   • `skills/1c-queries/references/queries-full.md` — «Лестница паттернов»;
   • `skills/1c-performance/references/performance-full.md` — «Граница правила»;
   • `docs/CHANGELOG.md` — запись [0.10.0] от 2026-08-15;
   • все три манифеста — 0.10.0.
3. 🔴 УРОК ДОСТАВКИ — ДВА КАНАЛА:
   • **ZCode**: для git-source маркетплейса НИ рестарт, НИ reinstall НЕ делают
     `git pull` клона. Чини: `git -C ~/.zcode/cli/plugins/marketplaces/1c-dev-rules
     fetch origin main` → `merge --ff-only origin/main`, ПЕРЕЗАПУСК клиента,
     reinstall. (Сессия 7: clone уже синхронизирован до 3b1faf6 — нужен только
     рестарт клиента + reinstall, если кэш ещё 0.9.0.)
   • **Kimi Work**: копия в ~/Documents/kimi/workspace/1c-dev-rules. ⚠️ УРОК
     СЕССИИ 7: rsync из терминала упёрся в macOS TCC (Documents — Operation not
     permitted). Либо дать терминалу Full Disk Access (System Settings →
     Privacy & Security → Full Disk Access), либо выполнить руками:
     `rsync -a --delete --exclude=.git ~/Projects/1c-dev-rules/
     ~/Documents/kimi/workspace/1c-dev-rules/`, затем перерегистрация
     (PersonalPluginRegister) и обновление в UI (вкладка «Личное»).
   Без этого скилы останутся 0.9.0; самопроверку можно вести по свежему main
   (Read из ~/Projects/1c-dev-rules/skills/...).

ЧТО УЖЕ СДЕЛАНО (НЕ повторяй):
• Сессии 1–2: обработка JSON, проведение резерва (PR #1–#2).
• Сессия 3: РЗ, HTTP-сервис, CFE (PR #3–#6). Сессия 4: подписки, безопасность
  (PR #8–#9). Сессия 5: отчёт СКД (PR #11). Сессия K: Kimi+аудит (PR #16–#20).
  Сессия 6: кейс E, форма списка, X2/X3/X4+F10 (PR #22–#23).
• Сессия 7: кейсы E'+P4, X15+X14, №732-лестница, №636-граница (PR #24).
  Починено суммарно: F1–F9, G1–G7', X11, G-EVT-1..4, F-G2, F-SEC-1, B-1/2/4/5,
  E-1…E-5, E'-1…E'-3, B-3/X14. См. CHANGELOG.md.

ЦЕЛЬ СЛЕДУЮЩЕЙ СЕССИИ — по выбору пользователя:
  a) ЭКСПЛУАТАЦИЯ: обычные прикладные задачи 1С в toir2 (или другом проекте)
     по правилам плагина — ловить отклонения плагина в повседневной работе;
  b) НОВЫЙ КЛАСС ЗАДАЧ, ранее не покрытый (например: xl-порты/обмен с внешними
     ИБ, мобильный клиент, БП-задачи, RLS-сложные схемы) — по появлению задачи;
  c) полировка по находкам эксплуатации.
  Через AskUserQuestion предложи варианты; по умолчанию — (a).

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — постоянная инструкция: правки плагина —
  обязательно commit+push; правки объектов ТОИР — БЕЗ коммитов):
  ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. В рабочем дереве
  правки сессий 1–7 (последние коммиты toir2 — docs:, прикладной код не
  закоммичен). Сессия 7 ДОБАВИЛА: ФормаМониторинга/Form.form — в queryText ДС
  поля гкс_КатегорияРиска + КритичностьДефекта и ЛЕВОЕ СОЕДИНЕНИЕ с
  торо_КатегорииРиска; в таблице 2 колонки (id 53–58). Правка вслепую без
  EDT-валидатора (правило 13) — проверить при импорте в EDT.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.10.0 — универсальные правила. Маршрутизатор: 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.

ПРОЦЕДУРА НА КАЖДУЮ ЗАДАЧУ:
1) Маршрутизация: 1c-dispatch-gate + docs/standards-cards/dispatch-map.md — оба.
2) Архитектурный чек-лист: 1c-arch-checklist + arch-checklist.md (7 вопросов).
3) Тематические скилы + карточки — читать текст, не по памяти.
4) Реализация в Project/Toir/src по правилам обоих слоёв.
5) Самопроверка: 1c-thin-triggers → 1c-code-review; для метаданных —
   `bash ~/Projects/1c-dev-rules/scripts/validate-new-object.sh <путь>`.
   Скрипт (0.10.0): 15 кросс-файловых проверок, включая X2/X3/X4/X15 (формы),
   X14 (СКД), X5/X6/X7/X8/X9/X11/X12/X13. FAIL — чини; ложное срабатывание
   скрипта — само по себе находка для плагина.
6) Отчёт об отклонениях (формат ниже).
7) Подтверждённое отклонение → фикс в ~/Projects/1c-dev-rules → верификация →
   `bash scripts/bump-version.sh patch|minor` → ветка feat/..., PR, мерж (gh,
   squash). После мержа — доставка (см. УРОК ДОСТАВКИ).

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции: не-100%-стандартная должна быть определена в проекте
  (grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl).
  `СообщитьПользователю` — функция БСП (ОбщегоНазначенияКлиентСервер).
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП — НЕ править. Буква ё в именах
  метаданных ЗАПРЕЩЕНА. Новые UUID — только случайные (uuidgen).
- 🔴 Правка метаданных XML вслепую — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА (главный артефакт):
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода>
  Предложение: <что добавить/исправить>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) ШАГ 0 — верификация версии/доставки до 0.10.0.
2) AskUserQuestion: цель сессии (a — эксплуатация по умолчанию / b — новый
   класс / c — полировка).
3) Выполняй выбранное по процедуре; отклонения — отчёт + фикс + PR.

ОЖИДАЕМЫЙ ИТОГ: эксплуатационная стабильность; точечные PR по находкам
(0.10.x — patch). Крупные новые классы — minor (0.11+) по мере появления.
