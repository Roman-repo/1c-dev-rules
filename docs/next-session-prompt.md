# Промпт для переезда в новую сессию — 4-е полевое тестирование плагина 1c-dev-rules

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, цель, список кейсов, процедура, стиль, формат отчёта.

---

Контекст: ЧЕТВЁРТОЕ полевое тестирование плагина 1c-dev-rules на реальных задачах 1С.
Продолжаем развивать репозиторий правил.

Рабочий каталог: ~/Projects/toir2 (1С:Предприятие 8 — ТОИР, EDT/БСП, платформа **8.3.27**
— подняли в 3-й сессии с 8.3.12, ~2822 BSL-модуля). Исходники: Project/Toir/src/.
Проект-донор для проверки плагина вживую.

ПЛАГИН: ~/Projects/1c-dev-rules (репозиторий-источник: github.com/Roman-repo/1c-dev-rules).
Версия на main: **0.5.4** (все 6 PR смёржены: #1–#6). Кэш ZCode, скорее всего, ещё
0.5.0 — см. ШАГ 0 (УРОК ДОСТАВКИ).

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ, до всего остального):
1. В СВОЁМ system-reminder проверь наличие 22 скилов 1c-* и сверь версию в путях.
   Нужно 0.5.4. Если кэш застрял на 0.5.0 — это ОЖИДАЕМо (3-я сессия не обновляла кэш,
   только main репо). Примени УРОК ДОСТАВКИ (ниже), затем перепроверь.
2. Маркеры 0.5.4 в кэше (после обновления):
   • scripts/validate-new-object.sh — содержит «X8» (methodName↔Module.bsl),
     «X11» (&ИзменениеИКонтроль↔ПродолжитьВызов), «os.walk» (правило 12 на Python);
   • scripts/validate-new-object.sh — case распознаёт CommonModules, ScheduledJobs,
     HTTPServices, WebServices (9 типов, не 5);
   • skills/1c-metadata/SKILL.md — секция «Общий модуль — от 2 областей» (G3 фикс);
   • skills/1c-events-transactions/SKILL.md — секция «Контроль отрицательных остатков»;
   • scripts/bump-version.sh — существует (маркер с 0.5.0).
3. 🔴 УРОК ДОСТАВКИ (подтверждён в 3-й сессии): для git-source маркетплейса НИ рестарт,
   НИ reinstall НЕ делают `git pull` клона. Чини так:
   • `git -C ~/.zcode/cli/plugins/marketplaces/1c-dev-rules fetch origin main`
     затем `git -C ... merge --ff-only origin/main`, ПЕРЕЗАПУСК клиента, reinstall;
   • либо пере-добавить маркетплейс через Settings → Plugin Management → Discover → «+»
     (GitHub-репо Roman-repo/1c-dev-rules) — принудительно пересоздаст клон.
   Без этого скилы в ZCode останутся 0.5.0; самопроверку кейсов можно вести по свежему
   main репозитория плагина (Read из ~/Projects/1c-dev-rules/skills/...).

ЧТО УЖЕ СДЕЛАНО В ПРЕДЫДУЩИХ СЕССИЯХ (НЕ повторяй — двигай дальше):
• Сессия 1: обработка торо_ВыгрузкаНоменклатурыВJSON. 2 runtime-блокера → PR #1.
  0.3.0 → 0.4.0. Добавлено: кросс-файловая §7 в 1c-thin-triggers + metadata-xml.md;
  scripts/validate-new-object.sh.
• Сессия 2: баланс-регистр торо_РезервЗапчастейНаРемонт + документ
  торо_КорректировкаРезерваЗапчастей с проведением по остаткам. 9 отклонений F1–F9,
  PR #2, 0.4.0 → 0.5.0.
• Сессия 3 (предыдущая): prep + 3 кейса (A/C/D), 8 отклонений, PR #3/#4/#5/#6,
  0.5.0 → 0.5.4. Подробности ниже — НЕ переоткрывай.

  УЖЕ починено в сессиях 1–3 (НЕ переоткрывай):
   - F1: drift marketplace.json ↔ plugin.json → bump-version.sh;
   - F2: баг `grep -c … || echo 0` → хелпер gc();
   - F3: недетерминированный выбор формы → проверка ВСЕХ форм;
   - F4: паттерн контроля остатков в 1c-events-transactions;
   - F5: гейт «контроль остатков → баланс-регистр» в 1c-dispatch-gate;
   - F6: исключение №783 для контроля остатков;
   - F7: X5 сканирует ObjectModule.bsl / ManagerModule.bsl;
   - F8: §6 M12 для accumulation-регистров;
   - F9: metadata-xml §1 LineNumber (EDT допустим / Конфигуратор — нет);
   - prep (PR#3): metadata-full.md — версия платформы обезличена (адаптивная формулировка,
     не хардкод 8.3.12 как факт проекта);
   - G1 (PR#4): validate-new-object.sh распознаёт CommonModules/ScheduledJobs + НОВЫЕ
     X8 (methodName↔Module.bsl Экспорт) и X9 (Schedule.schedule при predefined=true);
     X5 сканирует Module.bsl общих модулей; X7 пропускается для типов без объектных прав;
     BSD sed `-E` вместо `\?`/`\+`;
   - G3 (PR#4): «общий модуль — от 2 областей» (было категоричное «3 области», противоречило
     bslls:EmptyRegion и канону торо_ОтклоненияЗатрат);
   - G4 (PR#5): validate-new-object.sh распознаёт HTTPServices/WebServices + HAS_RIGHTS;
   - G5 (PR#5): X5 ловит латиничные имена параметров (`&[А-Яа-яA-Za-zЁё_]+`);
   - G6 (PR#5): правило 12 переписано на Python (os.walk по .mdo/.form/.rights) — было
     N×grep -rln, висло 5:54 на WebServices/Exchange (81 UUID) → стало 5с, ~70×;
   - G7' (PR#6): DIRECTIVES_RE дополнен CFE-директивами Перед|После|Вместо|ИзменениеИКонтроль
     (X5 ловил &После как параметр запроса);
   - X11 (PR#6, НОВОЕ): автоматическая проверка &ИзменениеИКонтроль↔ПродолжитьВызов
     (оригинал молча не выполнится).

  ОТЛОЖЕНО (можно подобрать в этой сессии):
   - G2 (🟡 minor): X7b WARN «объект не включён в подсистему» шумит на служебных регистрах
     (очереди, идемпотентность, адресаты рассылки — канон без подсистем). Нужна эвристика
     «служебный регистр» (напр. editType=InDialog без форм, или по составу измерений).

🔴 TOIR2-СЛЕДЫ 3-Й СЕССИИ (НЕЗАКОММИЧЕНЫ — решить с пользователем в начале):
  В ~/Projects/toir2 (ветка feat/refactor-gant-split-procedure) лежат незакоммиченные
  правки от 3-й сессии:
   - prep: Configuration.mdo (compatibilityMode 8.3.12→8.3.27), AGENTS.md (правило 11);
   - Кейс A: CommonModules/торо_НапоминанияОПлановыхРемонтрах (.mdo+Module.bsl),
     ScheduledJobs/торо_НапоминанияОПлановыхРемонтрах (.mdo+Schedule.schedule),
     InformationRegisters/торо_ОтправленныеНапоминанияОППР (.mdo), + регистрация в
     Configuration.mdo + роль в Roles/торо_БазовыеПрава/Rights.rights;
   - Кейс C: HTTPServices/торо_ОстаткиРезерваЗапчастей (.mdo+Module.bsl), секция
     <httpServices> в Configuration.mdo, роль HTTPService…Reserve.GET в БазовыхПравах;
   - Кейс D: Extensions/торо_ДоработкаАктаРабот/Documents/торо_АктОВыполненииЭтапаРабот/
     ObjectModule.bsl (перехватчик &После) + приближённый .mdo (в проекте нет образца CFE);
   - docs/standards-cards/thin-triggers.md (G3 правка).
  ВНИМАНИЕ: метаданные кейсов A/C/D созданы без EDT-валидатора (правило 13) — структура
  .mdo (особенно HTTPService и CFE — в проекте не было образцов) требует проверки при
  импорте в EDT. Сначала спроси пользователя: коммитить / вынести в отдельную ветку /
  откатить конкретные части — НЕ коммитить без его команды.

ЦЕЛЬ ЭТОЙ СЕССИИ — продолжить тестирование на КЛАССАХ, ещё НЕ покрытых, с ОБЯЗАТЕЛЬНЫМ
циклом по каждому кейсу:
  разработка в toir2 по правилам плагина → самопроверка → отчёт об отклонениях →
  фикс в репо плагина → верификация → коммит/PR.
Искать НОВЫЕ отклонения СВЕРХ F1–F9, G1–G7', X11.

СПИСОК КЕЙСОВ (разностороннее покрытие; каждый кейс = полный цикл):

✅ ВЫПОЛНЕНО (НЕ повторять): A — Регламентное задание (сессия 3); C — HTTP-сервис
   (сессия 3); D — CFE-перехватчик (сессия 3); + сессии 1–2 (обработка, проведение).

КЕЙС B — Отчёт на СКД (класс №3). 🔴 ПРИОРИТЕТ — единственный непокрытый из A–D.
  Задача: отчёт «Фактические затраты запчастей по объектам ремонта» на базе
  торо_ФактическиеЗатратыЗапчастей + торо_ОбъектыРемонта, с программным
  ПриКомпоновкеРезультата, параметрами периода, ВТ.
  Стресс: №670 (содержание), №733 (СрезПоследних/Обороты с параметрами), №655 (нет ВТ
  в соединении напрямую), №656 (нет ВЫБРАТЬ *), ПриКомпоновкеРезультата.
  Скилы: 1c-reports, 1c-queries + practice/dcs-design.md.

КЕЙС E — Форма (создание новой / доработка типовой БСП). Новый класс, почва для
  1c-forms: 5 обязательных областей, modifiesStoredData, условное оформление вместо
  кода, плоский динамический список.
  Задача-кандидат: форма списка/элемента справочника торо_ОбъектыРемонта с УО и
  динамическим списком; либо доработка формы документа торо_АктОВыполненииЭтапаРабот.
  Скилы: 1c-forms, 1c-ui-ux, 1c-performance (№489/№732).

КЕЙС F — Безопасность / RLS / привилегированный режим.
  Задача-кандидат: RLS-роль на регистр/справочник ТОИР (№689), или проверка
  УстановитьПривилегированныйРежим в существующем коде (№485), или роли для новых
  объектов кейсов 3-й сессии (доработать полноту — №532).
  Скилы: 1c-security.

КЕЙС G — Подписки на события / обработки табличных частей / обработчики.
  Задача-кандидат: подписка на событие записи документа ТОИР (№450/№464), или
  обработка ТЧ с групповой обработкой.
  Скилы: 1c-events-transactions, 1c-code-review.

ПОРЯДК ВЫБОРА: по умолчанию предлагай B → E → F → G (B — приоритет, непокрытый).
При сомнении — через AskUserQuestion. Желательно пройти 2–3 кейса за сессию.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.5.4 — универсальные правила разработки 1С без проектной специфики.
   Главный маршрутизатор: 1c-dispatch-gate.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.
   Специфика ТОИР: префиксы торо_/гкс_, карта движений (03_Документы_и_движения.md),
   бизнес-логика (торо_Ремонты, торо_РаботаСНаработкой, торо_ПланированиеРемонтов),
   очередь уведомлений (docs/standards-cards/practice/notifications-queue.md).

ПРОЦЕДУРА НА КАЖДЫЙ КЕЙС (после выбора):
1) Маршрутизация: 1c-dispatch-gate (плагин) ОДНОВРЕМЕННО с docs/standards-cards/dispatch-map.md
   (toir2) — пройди оба гейт-чек-листа.
2) Архитектурный чек-лист: docs/standards-cards/arch-checklist.md (7 вопросов) + 1c-arch-checklist.
3) Открой тематические 1c-* скилы + параллельные карточки toir2. Читай текст, не полагайся
   на память.
4) Реализация в Project/Toir/src по правилам обоих слоёв.
5) Самопроверка: 1c-thin-triggers → 1c-code-review (плагин), параллельно
   docs/standards-cards/thin-triggers.md и docs/code-review/ЧекЛистКодРевью.md (toir2).
   🔴 ЕСЛИ создаёшь/правишь метаданные — прогони
   `bash ~/Projects/1c-dev-rules/scripts/validate-new-object.sh <путь-к-объекту>` (dogfooding).
   Скрипт теперь (0.5.4) распознаёт 9 типов (Catalog/Document/DataProcessor/InformationRegister/
   AccumulationRegister/Constant/ChartOfCharacteristicTypes/CommonModule/ScheduledJob/
   HTTPService/WebService) и проверяет: UUID-дубликаты, синоним ru, конфигурационная
   регистрация (X6), роли №532 (X7), подсистему (X7b), параметры запроса &X↔УстановитьПараметр
   (X5), handler РЗ methodName↔Module.bsl (X8), Schedule.schedule (X9), CFE
   &ИзменениеИКонтроль↔ПродолжитьВызов (X11). Если скрипт упал или дал ложное срабатывание —
   это ТОЖЕ находка для плагина.
6) ОТЧЁТ об отклонениях (формат ниже) — по итогу каждого кейса.
7) 🔴 КОРРЕКТИВЫ В ПЛАГИНЕ: каждое подтверждённое отклонение → правка в ~/Projects/1c-dev-rules.
   • Верифицируй фикс (прогон скрипта на объекте toir2 / перечитка скила / grep-проверка).
   • Версию бампай ЧЕРЕЗ `bash ~/Projects/1c-dev-rules/scripts/bump-version.sh patch|minor`
     (НЕ правь plugin.json/marketplace.json руками — рассинхрон был инцидентом F1).
   • Коммит на новой ветке `feat/...` от main (НЕ коммить в main), конвеншен-коммиты,
     PR в main, мерж через gh (по аналогии с PR #1–#6). Подробно — AGENTS.md репо плагина.

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции. Не-100%-стандартная функция должна быть определена в проекте:
  grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl. Типичные галлюцинации:
  НайтиИлиСоздать, ОбщегоНазначения.СоздатьЭлемент, ЗагрузитьТабличныйДокумент.
- Платформа 8.3.27: Асинх/Ждать (нативный 8.3.18+), нативный xlsx (8.3.21+), CFE-роли
  (8.3.27+) — ДОСТУПНЫ. Сверяй compatibilityMode в Configuration.mdo при сомнении.
- Префиксы: прикладной код — торо_/гкс_; БСП (без префикса) — НЕ править (перезатрётся
  при обновлении).
- Буква ё в именах метаданных ЗАПРЕЩЕНА (только в пользовательских текстах).
- Новые UUID метаданных — только случайные (uuidgen | tr 'A-Z' 'a-z'), включая
  typeId/valueTypeId в producedTypes; проверка grep -rn "<uuid>" Project/Toir/src
  (0 дубликатов).
- 🔴 Правка метаданных XML вслепую (без EDT/MCP) — ОБЯЗАТЕЛЬНО предупреди пользователя:
  «эта часть не проверена валидатором EDT». Особо риск: HTTPService, CFE — если в
  проекте нет образца класса, предпочти создать через EDT New → ...
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА (главный артефакт сессии):
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода>
  Предложение: <что добавить/исправить в плагине — строка в thin-triggers / reference /
                дополнение скрипта / правка формулировки>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии
Отдельные важные классы (по опыту сессий 1–3):
- 🔴 Ложные отрицания (чек-лист НЕ поймал реальный баг — как кросс-файловые имена в
  сессии 1, баг grep -c в сессии 2, нераспознанные типы CommonModules/ScheduledJobs/
  HTTPServices в сессии 3).
- 🟡 Ложные срабатывания (чек-лист сработал там, где код корректен — как №783 на
  контроле остатков до F6, или X5 на &После / латиничных параметрах до G5/G7').
- 🔴 Противоречия между скилами/карточками (как «3 области» vs bslls:EmptyRegion — G3).
- 🟡 Устаревшие/неточные формулировки (как хардкод версии 8.3.12 — prep PR#3).
- 🟢 Пробелы в покрытии (класс задач без тематической карточки / проверки в скрипте —
  как methodName↔Module.bsl до X8, &ИзменениеИКонтроль↔ПродолжитьВызов до X11).
- 🟢 Производительность инструмента (как правило 12 на grep — G6).

С ЧЕГО НАЧАТЬ:
1) Выполни ШАГ 0 — подтверди версию кэша / примени УРОК ДОСТАВКИ до 0.5.4.
2) 🔴 Уточни с пользователем судьбу toir2-следов 3-й сессии (коммитить / ветка / откатить)
   — НЕ коммить без его команды.
3) Предложи пользователю выбор кейса (B приоритет / E / F / G) через AskUserQuestion.
4) По выбранному кейсу — двойная маршрутизация → разработка → самопроверка → отчёт →
   фикс в плагине → верификация.
5) Повтори для следующего кейса (цель 2–3 за сессию).
6) Финал: приоритезированный список отклонений + РЕАЛЬНО внесённые фиксы + верификация +
   бамп версии (через bump-version.sh) + коммит/PR в github.com/Roman-repo/1c-dev-rules.

ОЖИДАЕМЫЙ ИТОГ СЕССИИ: плагин 0.5.4 → 0.6+ с новыми проверенными фиксами (первый minor-бамп
после серии patch — если накопится существенно новый функционал, как полная поддержка СКД-
отчётов); каждый кейс оставил след и в коде toir2, и в улучшении плагина.
