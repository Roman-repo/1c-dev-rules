# Промпт для переезда в новую сессию — ЖИВАЯ БАЗА: ВНЕШНЯЯ ПРИЁМКА TASK-001 + TASK-002 (0.17.1)

> Скопировать целиком в начало новой сессии. Самодостаточен: контекст, что уже
> сделано, миссия, процедура, стиль, формат отчёта.

---

Контекст: плагин 1c-dev-rules **0.17.1** (main = мерж PR #44 — последний PR
сессии 11; все PR смёржены: #1–#44, в удалённом репо только main — ветки чистим
после мержа), **30 скилов** = 23 разработки + 7 конвейера (6 этапных +
`1c-epic-planning`, 0.17.0: Планирование → Проектирование → Разработка →
Внутренняя приёмка → Внешняя приёмка → Релиз; уровень выше — эпики). Конвейер
прошёл **два полевых прогона**, оба доведены до этапа «Внешняя приёмка» и ждут
живой базы: TASK-001 (сессия 9) и TASK-002 (сессия 11, из трекера).

Рабочий каталог для прикладных задач: ~/Projects/toir2 (1С:Предприятие 8 —
ТОИР, EDT/БСП, платформа 8.3.27). Исходники: Project/Toir/src/.
ПЛАГИН: ~/Projects/1c-dev-rules (github.com/Roman-repo/1c-dev-rules).
Двухплатформенный: ZCode + Kimi Work.

ИНСТРУМЕНТ: `python3 scripts/delivery_tools.py <status|check|roadmap>
<каталог>` — состояние и DoD-гейты из артефактов 01–06; roadmap — сводка всех
задач/эпиков каталога доставки (0.17.0).

ТРЕКЕР (с сессии 11): Project Control Tower на этой машине — http://localhost
(SPA; данные через API `/api/v1/*`, Bearer-токен: POST /api/v1/auth/login
c username/password). TASK-002 = задача 1030 (GCSRU-1763). Доступы локальные,
у пользователя — в репо НЕ писать.

ИСТОРИЯ (кратко; НЕ повторяй):
• Сессии 1–8: прикладные классы + скрипт X1–X15/F10 + слой конвейера (0.11.0).
• Сессия 9, PR #30–#32 (0.11.2–0.11.4): полевой прогон №1 — TASK-001, компактная
  печатная форма «Заявка на ремонт (согласование)». Отчёт:
  docs/field-reports/2026-08-15-conveyor-run-task001.md.
• Сессия 10, PR #34–#42 (0.12.0–0.16.1): ревью конвейера → 8 улучшений.
• Сессия 11 (2026-08-22): мерж 0.17.0 (PR #43: эпики, ADR, лимит ~7 файлов,
  границы правок 03); интеграция с трекером; полевой прогон №2 — TASK-002
  (GCSRU-1763 «Запрет статуса "Действует" техкарты без скан-копии», toir2):
  этапы 1–4 за одну сессию, 9/9 критериев ✅с статически; отклонение №1
  (легенда 04 → ложные ERR 14 на гейте 2→3) — фикс+тест, PR #44 (0.17.1).
  Отчёт: docs/field-reports/2026-08-22-conveyor-run-task002.md.

СОСТОЯНИЕ TASK-002 (~/Projects/toir2/docs/delivery/TASK-002/):
• 01–05 готовы, гейты 1→5 без ERR/WARN, этап 5. Матрица 04 — двухколоночная,
  9 критериев, «Статус 05» = ✅с везде; интерактивная проверка — на живой базе
  (инструкции в колонке «Проверка»).
• Код написан вслепую, БЕЗ EDT (3 файла): ManagerModule.bsl
  торо_ИдентификаторыТехКарт (ДопустимаУстановкаСтатусаДействует),
  ФормаЭлемента (проверки в СтатусОбработкаВыбора/ПередЗаписьюНаСервере —
  только на СМЕНУ статуса; обёртка НаСервереБезКонтекста; фильтр
  ФильтрФайловТехКарт в ДобавитьФайлОперации), торо_ОбщегоНазначенияКлиентСервер
  (ФильтрФайловТехКарт + ТекстОшибкиУтвержденияТехКартыБезФайла). 🔴 первый
  импорт EDT = обязательная проверка компиляции.
• ADR-001/ADR-002 — ~/Projects/toir2/docs/architecture/adr/ (первое применение
  ADR): проверка в модуле менеджера, НЕ в объектном (ломает миграции ИБ);
  группа «Ключевой пользователь» по наименованию через РС
  СоставыГруппПользователей, НЕ найдена → запрет для всех (fail-closed).
• Решения инициатора (зафиксированы в 01): наличие ЛЮБОГО файла снимает запрет;
  PDF только в фильтре техкарт (ТиповыеОР/ОбъектыРемонта не тронуты).

СОСТОЯНИЕ TASK-001 (~/Projects/toir2/docs/delivery/TASK-001/):
• Код и макет написаны вслепую (менеджер ЗаявкиНаРемонт + ПФ_MXL_Согласование);
  05 — статический прогон; внешняя приёмка ОТЛОЖЕНА (нет живой базы).
• 06 — «отложено» БЕЗ даты пересмотра (WARN от delivery_tools — закрыть при
  возобновлении). Матрица 04 — старый одно-колоночный формат (скрипт читает
  оба). Релиз: _releases/2.0.27.0-draft/.

ШАГ 0 — ВЕРИФИКАЦИЯ (выполни ПЕРВЫМ):
1. main = мерж PR #44 или новее (git log: «Merge pull request #44»); манифесты
   0.17.1; CHANGELOG [0.17.0..0.17.1].
2. 🔴 ДВА КАНАЛА ДОСТАВКИ:
   • ZCode: клон маркетплейса ~/.zcode/cli/plugins/marketplaces/1c-dev-rules
     отстаёт (клиент работал на кэше скилов 0.16.1 при main 0.17.x) —
     синхронизировать клон с main, ПЕРЕЗАПУСК клиента (+ reinstall, если кэш
     скилов в system-reminder < 0.17.1). До перезапуска: этапные скилы читать
     файлами из ~/Projects/1c-dev-rules/skills/ — репо = источник истины
     (практика сессии 11).
   • Kimi Work: копия в ~/Documents/kimi/workspace/1c-dev-rules ОСТАЛАСЬ на
     0.10.0 (TCC: Operation not permitted, сессии 7–11). rsync -a --delete
     --exclude=.git ~/Projects/1c-dev-rules/ ~/Documents/kimi/workspace/
     1c-dev-rules/, затем PersonalPluginRegister + обновление в UI.
3. delivery_tools status/check на TASK-001 и TASK-002 — сверить: обе на этапе
   5 «Внешняя приёмка», TASK-001 WARN про дату пересмотра (легален).

МИССИЯ СЕССИИ — ЖИВАЯ БАЗА: ЗАКРЫТЬ ОБЕ ЗАДАЧИ:
1) Пользователь импортирует toir2 в EDT (🔴 правки XML вслепую ОБЕИХ задач —
   обязательная структурная проверка при импорте: макет ПФ TASK-001 — индексы
   форматов/шрифтов, области; менеджер/форма/ОМ техкарт TASK-002) и запускает
   базу.
2) Внешняя приёмка TASK-002 по матрице 04 (критерии 1–9): запрет и сообщение;
   ключевой пользователь; PDF-загрузка; серверная защита (обход клиента);
   перезапись действующей карты; группа не найдена → запрет для всех; Типовые
   ОР без PDF. Для критерия 3/8 нужен тестовый пользователь в группе «Ключевой
   пользователь» (создать в базе). Результат → 06-acceptance-protocol.md,
   статусы приёмщика в «Статус 06» (✅/❌/⏳/n/a).
3) Внешняя приёмка TASK-001 по её матрице (критерии 1–7): меню Печать → вторая
   команда; поля сверкой; итоги vs смета; пустая заявка; нет цен. Результат →
   новый протокол 06-acceptance-protocol.r1.md со ссылкой на «отложенный» +
   🔴 дата пересмотра.
4) Возвраты по классам A/B/D → 06a-rework-list → правка → повторный прогон
   затронутых шагов. 🔴 Критерий ❌ дважды — следующая итерация закрывает его
   unit/BDD-тестом (0.16.0). Класс C — новая задача. При правке артефактов —
   формат 0.11.3/0.11.4.
5) После «принято»: повысить _releases/2.0.27.0-draft/ до боевого, состав —
   обе задачи; заполнить ретроспективу карточки 01 ОБЕИХ задач (оценка/факт,
   возвраты, вывод — DoD «6 → закрытие», 0.15.0); контрольный прогон
   delivery_tools check — без ERR/WARN (дата пересмотра TASK-001 закроет WARN).
6) Обновить статусы в трекере (http://localhost/tasks/1030 = TASK-002; источник
   TASK-001 уточнить у пользователя) — через UI или API пользователя.
7) Отклонения плагина, всплывшие на живом прогоне — отчёт (формат ниже) →
   фикс → PR → bump-version.sh patch. Известное наблюдение (кандидат на
   маленький PR): этап 3 «Разработка» невидим для статус-модели
   delivery_tools — нет артефакта-маркера, status показывает «Этап: 4» ещё до
   разработки (field-report 2026-08-22, «Наблюдения»).

🔴 TOIR2-СЛЕДЫ (НЕЗАКОММИЧЕНЫ — правки объектов ТОИР и АРТЕФАКТЫ ЗАДАЧ — БЕЗ
коммитов): ~/Projects/toir2, ветка feat/refactor-gant-split-procedure. Правки
сессий 1–10: ФормаМониторинга (ДС, колонки 53–58), менеджер/макет/mdo
ЗаявкиНаРемонт, docs/delivery/TASK-001. Правки сессии 11: менеджер + форма
торо_ИдентификаторыТехКарт, торо_ОбщегоНазначенияКлиентСервер,
docs/delivery/TASK-002, docs/architecture/adr/ADR-001/ADR-002.

ДВА СЛОЯ ПРАВИЛ — ИСПОЛЬЗУЙ ОБА (при расхождении проектный toir2 главнее):
1) Плагин 1c-* v0.17.1. Маршрутизаторы: задача → 1c-delivery-gate; код внутри
   этапа «Разработка» → 1c-dispatch-gate; инициатива дороже задачи →
   1c-epic-planning.
2) Проектный toir2 — AGENTS.md в корне + docs/standards-cards/ + docs/architecture/.

СТИЛЬ РАБОТЫ (критично для ТОИР — подробно в AGENTS.md):
- 🔴 Не выдумывай функции: не-100%-стандартная должна быть определена в проекте
  (grep -rn "Функция <Имя>" Project/Toir/src --include=*.bsl).
- Платформа 8.3.27: Асинх/Ждать, нативный xlsx, CFE-роли — ДОСТУПНЫ.
- Префиксы: прикладной код — торо_/гкс_; БСП — НЕ править. Буква ё в именах
  метаданных ЗАПРЕЩЕНА. Новые UUID — только случайные (uuidgen).
- 🔴 Правка метаданных XML вслепую — ОБЯЗАТЕЛЬНО предупреди пользователя.
- Комментарии — «почему», не «что».

ФОРМАТ ОТЧЁТА ОБ ОТКЛОНЕНИЯХ ПЛАГИНА:
  [Скил/Карточка/Скрипт] — что НЕ поймал (или поймал ложно)
  Пример: <конкретное место, файл:строка, фрагмент кода/артефакта>
  Предложение: <что добавить/исправить>
  Класс: 🔴 ложное отрицание | 🟡 ложное срабатывание | 🔴 противоречие скилов |
         🟡 устаревшее | 🟢 пробел в покрытии | 🟢 производительность инструмента

С ЧЕГО НАЧАТЬ:
1) ШАГ 0 — верификация доставки 0.17.1 (синхронизация клона + перезапуск
   клиента!) + delivery_tools status/check на обеих задачах.
2) Спроси пользователя: импортирован ли toir2 в EDT, готова ли живая база.
3) Если да — веди обе приёмки (сначала TASK-002 — свежая, меньше деталей
   потерять; затем TASK-001). Если нет — предложи помочь с импортом/запуском
   (диагностика EDT вне досягаемости — тогда зафиксируй паузу и спроси, чем
   занять сессию: фиксы-наблюдения из field-report 2026-08-22 (этап 3 в
   статус-модели delivery_tools); либо третий полевой прогон конвейера на
   новой задаче из трекера (http://localhost/tasks — список задач доступен
   через API) — не дожидаясь живой базы, как TASK-002).

ОЖИДАЕМЫЙ ИТОГ: 06-протоколы обеих задач = «принято» (или возвраты с классами
и цикл правки); релиз 2.0.27.0 повышен из черновика (состав: обе задачи);
ретроспективы 01 обеих задач заполнены; delivery_tools check обеих — без ERR;
статусы в трекере обновлены; отклонения живого прогона — отчёт+PR.
После закрытия обеих задач — обновить этот промпт (docs PR): следующая миссия
— по накопленным ретроспективам (третий полевой прогон на задаче из трекера
или первая эпик-декомпозиция).
