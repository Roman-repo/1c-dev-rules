# 1c-dev-rules

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Version: 0.5.7](https://img.shields.io/badge/version-0.5.7-blue.svg)](./docs/CHANGELOG.md)
[![Skills: 22](https://img.shields.io/badge/skills-22-green.svg)](#что-внутри)
[![Platform: ZCode](https://img.shields.io/badge/platform-ZCode-purple.svg)](#как-подключить)
[![1С:Предприятие 8](https://img.shields.io/badge/1С-Предприятие%208-red.svg)](https://v8.1c.ru)

> Универсальный подключаемый набор правил разработки **1С:Предприятие 8** для ИИ-ассистентов. Основан на стандартах [v8std](https://github.com/zeegin/v8std) и адаптирован под реальные грабли продакшен-разработки.

**Один плагин — много скилов.** Каждый скил = тематический набор правил с мини-чек-листом и развёрнутыми карточками в `references/` (progressive disclosure: скил в system-reminder короткий, детали подгружаются по требованию).

> **EN:** Pluggable set of 1C:Enterprise 8 development rules for AI assistants (ZCode/Claude/Codex). Packaged as skills based on the [v8std](https://github.com/zeegin/v8std) standards. Full docs below are in Russian — 1C ecosystem is primarily Russian-speaking.

## Оглавление

- [Что это и зачем](#что-это-и-зачем)
- [Чем это отличается от существующего](#чем-это-отличается-от-существующего)
- [Что внутри](#что-внутри)
- [Как это работает на практике](#как-это-работает-на-практике)
- [Требования](#требования)
- [Как подключить](#как-подключить)
- [Другие ассистенты](#другие-ассистенты-claude-code-codex-cursor)
- [Архитектура и пример скила](#архитектура-и-пример-скила)
- [Если что-то не работает (FAQ)](#если-что-то-не-работает-faq)
- [Лицензия и источники](#лицензия-и-источники)
- [Статус и roadmap](#статус-и-roadmap)
- [Обратная связь](#обратная-связь)
- [Контрибьюции](#контрибьюции)

## Что это и зачем

При разработке 1С ИИ-ассистент часто пишет «правдоподобный, но не по стандартам» код: `СрезПоследних` без параметров, чтение внутри транзакции, новый объект без ролей, плейсхолдеры вместо UUID. Этот набор заставляет ИИ **применять стандарты на лету** — через скилы, которые триггерятся по типу задачи и показывают конкретные правила.

**Главное:** это не справочник «почитать» — это набор, который **автоматически активируется** в контексте задачи. Модель видит описание скила в system-reminder и открывает его, когда задача подходит.

## Чем это отличается от существующего

| Источник | Что это | Чем данный набор отличается |
|---|---|---|
| [zeegin/v8std](https://github.com/zeegin/v8std) | ~360 стандартов разработки 1С как текст | Данный набор — **скилы**, адаптированные для триггеров ИИ: мини-сводка + чек-листы + примеры неправильно/правильно |
| [comol/ai_rules_1c](https://github.com/comol/ai_rules_1c) | Practice-карточки (public domain) | Переработаны: очищены от проектной специфики, перекрыты ссылками на v8std, добавлены мини-чек-листы |
| [cc-1c-skills](https://github.com/Nikolay-Shirokov/cc-1c-skills) | Спецификации форматов 1С | Используются как первоисточник для скилов метаданных/ParentConfigurations |

**Кратко:** v8std говорит «что правильно», `ai_rules_1c` — «как писать», а этот набор — **«как ИИ-ассистенту применять это на лету»** через механизм скилов.

## Что внутри

**22 скила** + 29 карточек-ссылок + 15 справочников стандартов v8std покрывают весь цикл разработки 1С:

### 🧭 Маршрутизация и процесс

| Скил | Назначение |
|---|---|
| `1c-dispatch-gate` | 🚪 **Главный маршрутизатор** — открывается первым. Гейт-чек-лист «сигнал → карточки» + таблица по типу работы |
| `1c-project-bootstrap` | 🚀 Генератор `AGENTS.md` и `.zcode/config.json` для нового проекта |
| `1c-arch-checklist` | 7 вопросов эксперта **до** написания кода |
| `1c-thin-triggers` | 🔥 Построчный чек-лист самопроверки по **5 слоям** |
| `1c-code-review` | Чек-лист ревью MR по секциям |
| `1c-patterns` | SOLID/GRASP/инженерные паттерны, приземлённые на 1С |

### 💻 Код

| Скил | Назначение |
|---|---|
| `1c-queries` | Запросы, СКД, динамические списки (№655, №733, №436, №656…) + проектирование СКД |
| `1c-forms` | Формы, архетипы, типовые, `modifiesStoredData`, 5 обязательных областей |
| `1c-metadata` | Метаданные, ручная правка XML (`.mdo`/`.form`), скелеты модулей, проектирование регистров |
| `1c-events-transactions` | Проведение, блокировки, транзакции (№783, №460, №648) |
| `1c-logging` | Журналирование, исключения (№499, №790) |
| `1c-async` | `Асинх`/`Ждать` (8.3.18+), запрет модальности (№703) |
| `1c-testing` | 🧪 Тесты: unit на YaXUnit, BDD на Vanessa Automation, цели покрытия |

### 🗄️ Данные, права, инфраструктура

| Скил | Назначение |
|---|---|
| `1c-performance` | Производительность, серверные вызовы, кэш, индексы |
| `1c-security` | Права, RLS, привилегированный режим, роли новых объектов (№532) |
| `1c-scheduled` | Регламентные задания (№539, №540), идемпотентность, устойчивость |
| `1c-platform-support` | `ParentConfigurations.bin`, редактируемость объектов БСП |
| `1c-integration` | Интеграция, HTTP, таймауты, обмен данными |

### 🎨 Вывод и UI

| Скил | Назначение |
|---|---|
| `1c-reports` | Отчёты, печатные формы |
| `1c-ui-ux` | Командный интерфейс, стилистика, UX |
| `1c-localization` | Локализация, `НСтр`, многоязычность |

### 🔧 Расширения

| Скил | Назначение |
|---|---|
| `1c-extensions` | Расширения (CFE), перехватчики `&Перед`/`&После`, `ПродолжитьВызов` |

## Как это работает на практике

Пример сессии в ZCode после установки:

```
Вы: Создай в конфигурации новый регистр сведений «ИсторияСогласований»

[ZCode видит в system-reminder скил 1c-dispatch-gate, срабатывает
 триггер «создаётся новый объект метаданных» → открывает скил]
  ↓
1c-dispatch-gate → гейт-чек-лист: «новый объект → 1c-metadata + 1c-security»
  ↓
1c-metadata: проверяет UUID (случайные, включая producedTypes),
             структуру модуля, синоним ru/en
1c-security: напоминает про №532 — включить в роль, даёт grep-проверку
  ↓
ИИ создаёт .mdo по правилам, а не «наугад»
```

Аналогично при ревью: стоит `Попытка/Исключение` без лога → `1c-thin-triggers` подсвечивает, `1c-logging` даёт шаблон с `ЗаписьЖурналаРегистрации`.

## Требования

- **ZCode** — любая версия с поддержкой plugins и skills (формат `.zcode-plugin/plugin.json`).
- **Модель** — любая, поддерживающая механизм skills (ZCode с моделями GLM/Claude/GPT — все работают).
- **Для Claude Code / Codex / Cursor** — см. раздел [«Другие ассистенты»](#другие-ассистенты-claude-code-codex-cursor) ниже.

## Как подключить

### Вариант 1 — плагин с GitHub (основной, ZCode)

1. В ZCode: **Settings → Plugin Management → Discover → `+`**
2. Вставьте URL: `https://github.com/Roman-repo/1c-dev-rules`
3. **Install** — плагин включён по умолчанию, все 22 скила сразу появятся в system-reminder

### Вариант 2 — как marketplace (удобно для команды)

Тот же URL подключается как marketplace (в репозитории есть `marketplace.json`), после чего плагины из него ставятся одним кликом. Удобно, если планируете развивать несколько плагинов.

### Вариант 3 — локальная разработка/тестирование

```bash
git clone https://github.com/Roman-repo/1c-dev-rules ~/Projects/1c-dev-rules
```

В `~/.zcode/cli/config.json` добавьте путь в `plugins.dirs`:
```json
{
  "plugins": {
    "enabled": true,
    "dirs": ["~/Projects/1c-dev-rules"]
  }
}
```

### Вариант 4 — Kimi (Kimi Work)

В репозитории есть манифест `kimi.plugin.json` — плагин можно зарегистрировать в личном маркете Kimi:

1. Скопируйте корень репозитория в рабочую папку сессии Kimi (требование личного маркета: корень плагина должен лежать внутри workspace) — например `~/Documents/kimi/workspace/1c-dev-rules`
2. Попросите агента зарегистрировать плагин («зарегистрируй плагин из 1c-dev-rules») — регистрация проверит `kimi.plugin.json` и добавит плагин в личный маркет
3. Установите плагин со страницы плагинов — все 22 скила `1c-*` появятся в индексе скилов

Обновление версии: скопируйте свежую версию в workspace и повторите регистрацию/установку (git-pull установленного плагина Kimi не делает — как и ZCode).

> ⚠️ Триггерные слова скилов продублированы в `description` — роутер Kimi отбирает скилы по `description`, поле `when_to_use` (ZCode) он не читает.

### Онбординг нового проекта

После установки в корне нового проекта вызовите:
```
/1c-project-bootstrap
```
Скил спросит префикс объектов, путь к исходникам, `compatibilityMode` — и сгенерирует `AGENTS.md` с универсальными секциями + заглушками под проектные детали.

## Другие ассистенты (Claude Code, Codex, Cursor)

Плагин оформлен в формате ZCode (`.zcode-plugin/plugin.json`). Для других ассистентов есть два пути:

**Быстрый (копирование скилов):** скопируйте каталоги из `skills/*/` в `.agents/skills/` вашего проекта — этот путь понимают Claude Code и Codex, а также ZCode (как workspace-scope). `SKILL.md` — это markdown с YAML frontmatter, формат совместимый.

**Адаптация манифеста:** ZCode принимает альтернативные имена каталога манифеста — `.claude-plugin/`, `.codex-plugin/`, `.cursor-plugin/`. Можно сделать символическую ссылку или скопировать `plugin.json` под нужным именем.

> ⚠️ На сегодня **протестировано в ZCode и Kimi** (для Kimi есть штатный манифест `kimi.plugin.json` — см. «Вариант 4»). Для Claude Code/Codex/Cursor формальная совместимость есть, но триггеры не проверялись в реальных клиентах.

## Архитектура и пример скила

```
1c-dev-rules/
├── .zcode-plugin/plugin.json   ← манифест плагина ZCode
├── kimi.plugin.json            ← манифест плагина Kimi (версия бампится тем же bump-version.sh)
├── skills/                     ← 22 скила (каждый = каталог)
│   └── <name>/
│       ├── SKILL.md            ← frontmatter + мини-сводка (≤500 строк)
│       └── references/*.md     ← полные карточки, progressive disclosure
├── scripts/                    ← validate-new-object.sh (X1–X13), validate_skills.py, bump-version.sh
├── std/                        ← 15 оглавлений v8std (справочный слой)
└── docs/                       ← ARCHITECTURE, SOURCES, CHANGELOG
```

Принцип: **универсальные правила здесь, проектная специфика — в `AGENTS.md` проекта**. Скилы не содержат упоминаний конкретных конфигураций (`торо_`, `Project/Toir`) — только универсальные правила и примеры.

### Как устроен один скил (`SKILL.md`)

```markdown
---
name: 1c-queries
description: Запросы 1С, СКД, динамические списки. Use when writing/reviewing
  query/DCS/dynamic-list code: №733 (СрезПоследних с параметрами), №655 (нет ВТ
  в соединении). Мини-чек-лист в конце.
when_to_use: При написании или ревью запросов. Триггеры: "СрезПоследних",
  "ЛЕВОЕ СОЕДИНЕНИЕ", "ВЫБРАТЬ *".
license: MIT
---

# 1c-queries — запросы, СКД

## Краткая сводка критичных правил
...мини-сводка стандартов...

## Мини-чек-лист
- [ ] правило 1
...

## Детали (читать по необходимости)
- references/queries-full.md — полная карточка (19 стандартов)
```

Правила оформления скилов (поля frontmatter, длина description, очистка от проектной специфики) — в [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md).

## Если что-то не работает (FAQ)

### Поставил плагин, а скилы не появились в system-reminder

1. **Проверьте, что плагин включён:** Settings → Plugin Management — должен быть в списке со статусом enabled.
2. **Запустите диагностику ZCode:**
   - `/zcode-guide:diagnosing-plugins` — корректность установки плагина (манифест, путь, включён ли).
   - `/zcode-guide:diagnosing-skills` — корректность обнаружения скилов (frontmatter, `description` ≤1024 симв., `name` совпадает с каталогом).
3. **Перезапустите сессию** — список скилов строится в начале хода; после установки нового плагина нужен новый сеанс.
4. **Проверьте права:** плагин должен быть включён для текущего workspace или глобально (см. `~/.zcode/cli/config.json` → `plugins.enabledPlugins`).

### Скилы появляются, но не триггерятся автоматически

- `description` — главный триггер. Если скил under-triggers, проверьте, что описание начинается с «Use when…» и содержит конкретные сигналы. Лимит — первые ~250 символов; всё важное должно быть в начале.
- Можно вызвать скил явно: `/1c-queries` или через меню `/` → Skills.

### Скил загрузился, но контент не соответствует моему проекту

Это задумка: скилы **универсальны** (без привязки к конкретной конфигурации). Проектная специфика (префикс, карты объектов, каноничные чтения) должна лежать в `AGENTS.md` вашего проекта. Сгенерировать его: `/1c-project-bootstrap`.

### Хочу отключить часть скилов

В `~/.zcode/cli/config.json` → `plugins.enabledPlugins` укажите плагин как `false`, либо используйте UI: Settings → Plugin Management → отключить.

## Лицензия и источники

**MIT** — см. [LICENSE](./LICENSE). Набор можно использовать, модифицировать, распространять (включая коммерческое использование), с сохранением notices.

Контент адаптирован из трёх открытых источников (подробно — [docs/SOURCES.md](./docs/SOURCES.md)):
- **[zeegin/v8std](https://github.com/zeegin/v8std)** — ~360 стандартов разработки 1С
- **[comol/ai_rules_1c](https://github.com/comol/ai_rules_1c)** — practice-карточки (public domain)
- **[Nikolay-Shirokov/cc-1c-skills](https://github.com/Nikolay-Shirokov/cc-1c-skills)** — спецификации форматов 1С

## Статус и roadmap

**Текущий: ✅ v0.5.9** — 22 скила + 29 references + 15 std + автоварификатор скилов (`scripts/validate_skills.py`) + **скрипт автопроверки метаданных `scripts/validate-new-object.sh`** (распознаёт 13 типов объектов, сверяет кросс-файловые цепочки X1–X13 при ручной правке XML без EDT — UUID/роли/подсистемы/обработчики/схемы СКД) + `bump-version.sh` + CI + **манифест Kimi (`kimi.plugin.json`)**. Карточки обезличены, без привязки к конкретным конфигурациям. Плагин прошёл **5 полевых тестирований** на реальном проекте 1С (обработки, проведение, РЗ, HTTP/CFE/подписки/безопасность, отчёты СКД) — каждый цикл «реальная задача → dogfooding → фикс правил».

| Статус | Что |
|---|---|
| ✅ Готово | Каркас плагина, 22 скила, публикация на GitHub, 5 полевых тестов, `validate-new-object.sh` (13 типов, X1–X13) |
| 🧪 Тестируется | Совместимость с Claude Code / Codex / Cursor |
| 📋 Планируется | Полевые тесты оставшихся классов (формы); X14 (согласование `&параметр↔<parameter>` в СКД); расширение списка типов в `validate` |
| 💡 Идеи | Локализация карточек на английский; интеграция с BSL Language Server |

Журнал версий — [docs/CHANGELOG.md](./docs/CHANGELOG.md).

## Обратная связь

- 🐛 **Баги, проблемы, идеи** — [GitHub Issues](https://github.com/Roman-repo/1c-dev-rules/issues). Перед созданием проверьте, что баг воспроизводим; приложите вывод `/zcode-guide:diagnosing-skills` если вопрос про скилы.
- 💬 **Вопросы, обсуждения** — [GitHub Discussions](https://github.com/Roman-repo/1c-dev-rules/discussions) (если включены) или Issues с пометкой «question».
- 🔀 **Вклад** — см. раздел [Контрибьюции](#контрибьюции) ниже.

## Контрибьюции

PR приветствуются. Перед правкой карточки:
1. Сверьтесь с upstream (v8std / ai_rules_1c), чтобы не внести расхождение с первоисточником.
2. Прочитайте [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md) — там правила оформления скилов.
3. Проверьте: `name` = имя каталога, `description` ≤1024 симв. (скил дропается сверх этого; ключевые сигналы — в первых ~250, см. FAQ), нет упоминаний конкретных проектов.
